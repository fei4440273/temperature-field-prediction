# SiC–Cu 温度场预测：M0/M1 诊断后的优化执行方案 V5

> 用途：交给 Codex，在现有工程上继续执行。本文新增的是诊断与受控对照任务，不是已完成的训练结果。  
> 日期：2026-09-10。  
> 依据：用户提供的 V4 M0/M1 诊断报告；本次核对的公开仓库提交 `bbe563e05ee65e28362ccab04d91773896d8d0c4`。公开分支尚未包含可独立读取的本轮全部诊断产物，因此本轮数字以用户粘贴报告为依据，未重新运行真实检查点。  
> 不变事项：高保真12/3/3、低保真60/10/10；不启用LOGO；不读取test_Data温度开展研发；保留完整SiC/Cu仿真；物理约束作用于最终高保真场。

## 0. 决策摘要

**暂缓 E1/E2 时间特征实验。先完成能量审计链路与低保真主干核验，再做优化排程和联合训练对照，最后才进入时间表示改进。**

不更换 DeepONet，不要求PRC必须胜出，不因一次梯度夹角大于90度直接启用复杂梯度算法，不把物理损失乘10/100作为默认修复。

现有B0是可复现的“冻结低保真先验 + 高保真校正”模型，仍属于多保真模型。它不是已经由验证选中的联合微调模型，也不能因为训练日志出现联合更新就声称部署结果来自联合微调。

本轮先做以下两件无新训练任务：

1. **M2-E：确定性能量核算与水冷通量求导审计。**
2. **M2-L：低保真检查点来源、评价口径与分材料拟合误差审计。**

通过后再执行 **M3-S：排程对照** 和 **M3-J：受限联合微调**。新特征最后执行。

---

## 1. 已确认事实与不能推出的结论

| 报告事实 | 可以得出的结论 | 不允许据此推出 |
|---|---|---|
| macro_v1 = 2.0016 ± 0.0667 ℃，复现差最大1.136e-06 ℃ | 指定检查点的验证评价可复现 | 历史训练代码和训练过程已逐位复现 |
| 所有入选最佳轮次早于1501，LF张量与预训练完全相同 | 当前部署使用冻结LF，不存在部署检查点内的LF遗忘 | 没用仿真；联合微调必然有害；联合阶段没有运行 |
| LF验证 Cu=4.2522 ℃、SiC=9.3110 ℃ | 当前配对LF主干的全域拟合质量必须优先检查 | 与其他历史指标不核对口径即可断定回退或代码错误 |
| 257 W热环t=0为25.2890 ℃，物理IC=22 ℃ | 存在可定位的初始观测与模型条件冲突 | 全部曲线应减去3.2890 ℃；全部实验真实初温都为25.2890 ℃ |
| 其他首个环温多在t=1，均值高于22 ℃约3.3892 ℃ | 需要区分真实升温、初温差、同步及测量偏置 | 首个1秒温度就是初温，或均值差就是标定偏置 |
| 数据/物理更新为25500/1700=15:1 | 更新日程不对称，需要受控检查 | 物理影响恰好小15倍，或必须提高15倍权重 |
| 固定点梯度夹角约121–132度 | 在该状态的欧氏梯度存在方向冲突 | 整个训练期间持续冲突，或Adam实际更新一定相互抵消 |
| 能量相对不平衡0.714–0.909 | 全局物理预算尚未得到可靠闭合证据 | 直接认定真实装置损失了71%–91%的热量，或把它视为测得的内部误差 |
| 初始损失恒为零 | 硬初值在结构上已满足，损失可能冗余 | 初温与实验必然一致；这个零损失贡献了训练改善 |
| 批大小误差0.000122 ℃≈4 ULP | 温度推理的批量一致性不是当前主瓶颈 | 所有高阶导数和通量都已精度充分 |
| 配置哈希仅元数据键名变化 | 应保留原始与数值语义两套指纹 | 修改历史哈希使其看似从未变化 |

另外：归一化PDE RMSE约0.0993/0.0567不是9.93%/5.67%的温度误差，也不能直接和能量相对误差相比。必须发布归一化定义及对应有量纲残差。

---

## 2. M2-E：优先校验能量审计，禁止先用错误能量残差训练

### 2.1 优先检查水冷边界覆写

公开代码 `src/sic_cu/models/multifidelity.py` 在启用硬水冷约束时含有：

```python
output = torch.where(copper & outer_radius, cooling_temperature, output)
```

若在满足 `outer_radius` 的节点对这个最终输出求径向导数，自动微分返回选中常数分支的导数0。固定边界温度不代表法向导热通量为0。因此必须检查本轮能量审计：

- 实际检查点是否启用该覆写；
- 审计是否在r=R或isclose容差带内对覆写后的输出求导；
- 是否已有单侧内极限或未覆写分支路径；
- 模型在r→R−的温度极限是否等于水冷温度。

本次已运行一个独立合成单元例子（不是实际检查点）：

```text
T_raw = 295.15 + 100 * (R-r)
在r=R处：原始温度=覆写温度=295.15 K
原始法向梯度=-100 K/m；覆写后的自动微分梯度=0
取k=401 W/(m K)：原始外向通量40100 W/m²；覆写通量0
```

这证明该实现路径可以在边界温度完全正确时仍使通量错误；不证明本轮0.714–0.909全由它造成。

**诊断要求：** 对若干训练/验证功率和时刻，计算r=R−ε的温度与径向导数，ε取几何尺度的对数序列且明确大于isclose容差。不要仅用 `(T(R)-T(R-ε))/ε` 掩盖不连续；同时分别检查内侧导数和温度极限。

若内侧温度不能连续达到边界值，不能通过换一种能量求导算法就宣布模型已修复。后续训练应采用连续边界表示，或显式边界损失，而不是只在边界点覆写输出。

### 2.2 确定性轴对称求积

几何沿用仓库，z向下为负。拆成三个无重叠矩形截面：

- SiC：r∈[0,0.025]，z∈[−0.012,0]。
- Cu外环：r∈[0.025,0.05834]，z∈[−0.0175,0]。
- SiC下方Cu：r∈[0,0.025]，z∈[−0.0175,−0.012]。

体积积分权重为 `2πr dr dz`；水平圆盘/环面面积为 `2πr dr`；半径R的外圆柱面为 `2πR dz`。轴线面积为0。不能用FEM节点等权和代替体积积分，不能把两个材料界面当作外界重复散热。

对连续坐标网络直接做Gauss–Legendre求积；每个子域采用16/32/64阶作收敛检查，面上同步加密。精度不足则继续加密，而不是固定宣布64阶足够。用float64完成审计的求和与必要求导，不要求全部训练切换float64。

先对固定训练功率55/364.3/729 W和所有验证功率运行；这些点不是从测试误差选出的。物理查询可在整个既定0–200秒范围内，观测评价仍仅限真实采集时刻。t=0的阶跃角点单独作为初值测试，不并入常规瞬态通量收敛判定。

### 2.3 发布原始瓦数和两种互补恒等式

固定无相变几何、当前常热物性下：

\[
\dot E(t)=\sum_m\int_{\Omega_m}\rho_m c_{p,m}\partial_tT_m\,dV.
\]

温变热容扩展时使用热焓积分，不直接把cp(T)T当作内能。

工程热预算：

\[
B(t)=\dot E+Q_{cool}+Q_{conv}+Q_{rad}-P_{abs}.
\]

各项正负定义写入表头。水冷项取模型内侧外向导热通量；自然对流和辐射仅覆盖对应外表面；激光不同时作为边界输入和体源重复加入。

发布至少：

```text
power_w,time_s,quadrature_order,
absorbed_power_w,storage_rate_w,cooling_heat_w,
convection_heat_w,radiation_heat_w,balance_w,
relative_balance_denominator_w,relative_balance,
boundary_gradient_mode,quadrature_change_w
```

额外做数学散度定理审计：

\[
R_m=\rho_m c_m T_{m,t}+\nabla\cdot q_m,\qquad q_m=-k_m\nabla T_m.
\]

令J为材料界面两侧各自外法向通量之和的积分，则应有：

\[
\sum_m\int_{\Omega_m}R_m\,dV
=\dot E+\int_{\partial\Omega_{ext}}q\cdot n\,dA+J.
\]

这里的外界通量全部从模型梯度计算；它与按边界定律计算的工程热预算不同。两者之差应由边界通量残差和J解释。如此可区分求积/符号错误、界面热流不连续、边界失配和内部PDE误差。

若P=0或分母很小，主要报告W，不能通过人为极小分母放大相对比值。

### 2.4 高斯热源面积校验

当前代码采用无限平面归一化高斯：

\[
q(r)=\frac{2\eta P}{\pi w^2}\exp(-2r^2/w^2).
\]

若只作用在半径Rs的SiC顶面，则：

\[
P_{abs}=\eta P[1-\exp(-2R_s^2/w^2)].
\]

Rs=0.025 m、w=0.02 m时截获比例约0.956063。是否需要截断后重归一化、Cu顶面是否也吸收外溢光，必须遵循已确认的激光功率语义及原仿真设置，不能静默更改。该截断因子自身只有约4.4%的差异，不能单独解释接近0.8的相对不平衡。

### 2.5 三类控制测试

1. 零功率等温场：全部温度T0、全部梯度0，各项应为0。
2. 已知解析函数/制造解：用已知梯度、体源和边界通量验证2πr、法向、散度定理及时间导数。测试用制造源不进入真实训练数据。
3. 原有限元参考：有原生单元热流/边界反力热流则优先使用；仅有节点温度时采用明确的分材料重建并评估导数离散误差。参考FE先按自身边界配置核算，不能把LF与名义HF边界不同误判成FE数值不守恒。

建议数值验收：相邻两级求积的能量项变化相对吸收输入低于1%作为初始求积收敛目标；P=0用绝对容差。该目标只判断积分收敛，不代表真实模型物理误差合格。物理闭合容忍度应在看新结果前结合参考解离散误差、边界近似和已知测量不确定度确定。

若正确求积后仍存在很大且稳定的不闭合，暂不将模型标为内部物理可信；必须通过模型/边界/训练改进解决或限定结论。禁止先把未经验证的B加入强惩罚，逼模型“满足审计器”。

---

## 3. M2-L：核查低保真完整场质量

当前LF Cu=4.2522 ℃、SiC=9.3110 ℃值得排在新特征之前处理。但不能未经核对直接与历史0.538 ℃等不同口径结果相比。

每个配对LF检查点生成：

- 文件路径、SHA-256、method、训练功率清单、预训练epoch、来源快照；
- 调用明确的 `fidelity="low"`/`forward_low`，确认没有评价到HF结果；
- 仿真验证集的材料标签、节点对齐、坐标mm→m、z负方向、K/℃、真实时间及容差；
- 全域与SiC/Cu分开、各时间窗、逐功率误差；
- 查询方式与权重：直接节点查询、节点/体积权重区别、宏平均规则；
- 与真实仿真节点一致的预测和误差抽查。

若发现检查点/单位/标签/评价错误，先修复评价或来源并重算，历史结果保留。

若确认LF主干确实拟合不足：**先只用simulation train修复LF代理模型，不读取HF验证温度来选择LF拟合。** 复用已正确实现的DeepONet；优先检查按功率、材料、时间分层采样及收敛，不先扩大网络。

在同一低保真验证口径下比较当前LF和一个修复后LF，最多两份候选；从各自LF开始用相同HF校正结构、种子和预算训练对照，确认LF改善是否传导为HF验证改善。LF拟合更好不自动保证HF结果更好。

LF可先以数据监督学习模拟系统；若使用LF物理约束，必须对应模拟系统本身。不要把有已知偏差的HF名义边界强加给LF，迫使LF偏离其仿真标签。

始终保持：

\[
L_{LF}=\ell(\widehat T_{LF},T_{sim}),\quad
L_{HF}=\ell(H[\widehat T_{HF}],y_{exp}),\quad
L_{phy}=L_{phy}(\widehat T_{HF}).
\]

其中H为顶面/环温观测算子。模拟标签不强制约束最终HF场贴回模拟结果。

---

## 4. M2-O：初始状态与测量观测检查

### 4.1 当前只确认一条精确t=0冲突

257 W热环的25.2890 ℃与22 ℃硬初值相差3.2890 ℃。只要模型硬固定22 ℃且观测算子没有偏置，该点温度误差无法由改变网络权重消除。

其余多条曲线首点是1秒。不能把该首点温升、传感器偏置和真实初温混为一谈；更不能对所有数据统一减3.3892 ℃。

### 4.2 执行顺序

先查看训练集原始表头、已有采集日志和标定资料；利用已有激光开启前记录核验。没有资料则明确保持未辨识，不再向数据里制造t=0帧。

- 若证明真实初始温度不同，模型初始条件应反映实际状态；仅当该状态是实际部署前可获得的输入时才条件化T_init。不要偷偷从测试温度反推T_init后称为只输入功率。
- 若证明某个通道有稳定标定偏差，可在观测层使用 `y_s=H_s[T]+b_s`。b_s按通道共享、约束范围来自已有标定证据，只用训练拟合，不对每个验证/测试功率设自由偏置。
- 若无法区分，不修改22 ℃名义初值、不删首点、不增加自由偏置。把不兼容项作为模型失配记录，绝对温度与温升结果并列。

模拟t=0为22 ℃只证明模拟初始设置，不能单独证明所有实验真实初温恰好22 ℃。

硬IC恒零时，将其作为结构单元测试和数值监测；不用对该恒零项做自适应损失归一化或梯度倒数加权，防止除零/无意义权重。

---

## 5. M3-S：训练排程的最小对照

M2中发现的确定实现错误先解决；能量审计至少数值可信。保持B0原工件，不在原文件上修改。

### 5.1 先澄清冻结LF的诊断梯度

报告中冻结LF仍有非零“物理梯度”，应标明是否临时启用requires_grad计算反事实梯度。它是“若允许更新，物理目标希望怎么改”，不是实际优化器在冻结LF上执行过的更新。

诊断函数必须保存/恢复requires_grad、train/eval状态，并断言检查点权重未改变。发布梯度所用参数集、loss缩放、是否预条件、batch及seed。欧氏梯度夹角与Adam预条件后的真实位移不等价。

### 5.2 两个排程先比较，暂不解冻LF

| 名称 | LF参数 | 数据/物理排程 | 作用 |
|---|---|---|---|
| S0 | 冻结 | 当前15个数据步后1个物理步 | 当前源码下的排程对照 |
| S1 | 冻结 | 周期性混合损失，如每5个数据步加入一次物理 | 检验排程，不引入特征和新网络 |

旧B0历史源码缺失，所以新S0不能宣传成旧训练逐位复现。S0/S1从相同LF、相同HF初始化和相同随机种子开始。

预算至少匹配/记录：HF样本暴露次数、LF查询次数、物理残差计算总点数、边界和界面各自配点数、优化器步数、墙钟与显存。不要因为每次物理调用的最小边界配点数导致预算暗中扩大。

若将每个宏周期的一次物理批次拆成K次，为隔离排程影响，按积分权重归一化并使总物理系数贡献相当，而不是把同一λ重复K次而不说明；平均损失可按样本比例或1/K分摊。即便这样，Adam轨迹仍会不同，这正是要比较的排程效应。S0是16个更新而S1可能15个混合/数据更新，该差异也必须记录，不能声称每个细节都完全相等。

先用seed0做不超过预登记预算的短排程诊断，观察多个固定训练批次在多个时点的梯度夹角分布、负内积比例和范数；短跑不用于宣称最终精度。选择有依据的排程再用同等完整训练预算复核。

若仍存在持续冲突，先分解数据与各物理分量并检查物理失配；之后才试一种带限幅/滑动平均的权重调整。暂不同时启用PCGrad、因果权重、全局能量罚项和新采样策略。

---

## 6. M3-J：保证联合阶段可审计，而不是强迫联合模型获胜

### 6.1 增加分阶段产物

每个run至少保留：

```text
initial_state.pt
correction_best_observation.pt
correction_end.pt
joint_start.pt
joint_best_observation.pt
joint_best_feasible.pt       # 可行性门槛未定义/未通过则不生成，写原因
joint_last.pt
training_last.pt
```

检查点包含模型、优化器、学习率状态、随机状态、stage、有效配置、来源清单、LF/HF可训练参数清单、LF/HF/物理验证指标。联合阶段另按固定步数保存中间检查点；磁盘预算在配置中限制。

即使全局best还留在校正阶段，仍要保存联合阶段真实结果。不得仅复制校正best并改名joint。

### 6.2 仅新增一个联合候选

S2从选定排程下的同一个 `correction_end.pt` 出发，先只解冻DeepONet输出投影层/最后一块与HF校正器，保留仿真训练回放。冻结版本S1从相同点继续相同后续预算，作为真正的联合对照。

建议起始学习率比例 `lr_LF = 0.1 * lr_HF`，这是候选，不是最优值。先保留时间特征不变，不同时重拟合吸收率、发射率、光斑和接触热阻。

每次验证同时输出：

- 原macro_v1及Top/Hot/Cold绝对/温升误差；
- 当前LF分支在simulation validation上的Cu/SiC误差；
- 独立PDE/各边界残差与通过审计的能量预算。

若LF快速变差、HF无收益，保留冻结路线。多保真并不要求LF每轮都更新；能否使用仿真由前向与训练来源确定，不由joint标签确定。

### 6.3 双轨保存与选择

保留原macro_v1为历史可比指标，同时保存“观测最优”和“满足预先登记物理/保真门槛后的观测最优”。

物理门槛必须在能量审计控制测试和参考误差确认后确定并归档，不能从候选结果倒推。未通过就明确无可行模型；不能把同一组较差数值标成已通过。

第一轮排程/联合修复可接受观测分数基本保持而物理预算明显改善，不必强求立刻比2.0016下降5%。例如可预登记：macro相对基线恶化不超过2%、单模态不超预定容忍度，同时正确能量残差明显下降；具体阈值根据诊断可靠性固定。这是研发筛选，不是统计显著性或投稿门槛。

算法精度创新阶段才回到5%改善、4/5种子同向等既有研发门槛；不要为降低观测分数选择能量失真的检查点。

---

## 7. 是否增加全局能量损失：只在审计后决定

若数学积分恒等式成立、边界通量正确、参考解闭合，而当前模型仍显著不闭合，可先试低权重积分约束：

\[
L_E=\frac1{N_PN_t}\sum_{P,t}\left(\frac{B(P,t)}{Q_{ref}(P)}\right)^2.
\]

选择一个已声明的、与源功率相关且非零的尺度，发布有量纲W，不靠调分母缩小指标。

也可使用时间窗积分能量式：

\[
E(t_b)-E(t_a)-\int_{t_a}^{t_b}[P_{abs}-Q_{out}]\,dt=0,
\]

减少对瞬时时间导数的依赖，但它不能修复错误外边界通量。第一版只选一种形式，不同时增加两种loss；权重从0开始小范围验证。

积分约束只能检查/约束总能量，不保证空间场正确；仍保留局部PDE、分材料条件和真实观测损失。若加入守恒约束明显恶化各模态，回到物理边界/观测失配，而不是继续增大权重。

---

## 8. 何时返回E0/E1/E2

以下条件满足后才开始：

1. 能量审计通过控制测试并有求积收敛、正确的水冷通量路径；
2. LF主干来源与验证质量明确，不能用错误/不合格来源继续生成响应字典；
3. 选定了可复现的训练排程与阶段保存规则；
4. 257 W初始冲突已有证据分类或明确保留限制，没有测试驱动修正；
5. 观测—物理—LF保持三条指标能够共同监测。

届时仍保持三个版本：无额外特征、固定时间尺度特征、LF训练场拟合的时间尺度特征。相同LF、预算和种子。只有第三版优于第二版，才支持“从LF提取热响应信息”这一特定机制的价值。

本轮报告Top初期RMSE5.0637 ℃、晚期3.8006 ℃、外圈3.8767 ℃说明后续应同时关注早期与晚期/径向误差；不能只加快模式便宣称已经解决全部问题。两材料仍共享完整场物理，不能为降低Top分数牺牲Cu曲线。

---

## 9. 文件级交付与单元测试

新增路径均为计划，不声称仓库已经存在；若本地已有同功能实现，应扩展而不是重复创建。

```text
reports/development_v5/
  m2_energy_flux_audit.md
  m2_energy_terms.csv
  m2_quadrature_convergence.csv
  m2_divergence_identity.csv
  m2_lf_checkpoint_comparison.csv
  m2_initial_observation_audit.md
  m3_schedule_comparison.json
  m3_stage_checkpoint_index.json
  m3_validation_pareto.csv
  decision_log.md
```

建议代码落点：

- `src/sic_cu/eval/`：确定性求积、边界单侧极限、散度恒等式、分阶段LF保持。
- `src/sic_cu/models/multifidelity.py`：仅在确认问题后实现平滑边界表示；不事后覆盖旧模型改变测试结果。
- `src/sic_cu/train/multifidelity.py`：周期混合更新、分参数组优化器、分阶段检查点。
- `src/sic_cu/physics/boundary.py`：高斯有限圆盘积分单元测试，不静默改变热源语义。
- `configs/`：独立V5配置和有效源码/数据清单；旧配置不覆写。

必须测试：

1. 固定温度边界不能被错误推断为零法向热流；覆写版在合成场中应显式暴露这个失败。
2. 2πr体积/面积、界面双侧法向、工程热预算不重复计源。
3. 恒温场、解析场/制造解的积分收敛。
4. 冻结LF无参数更新但保留坐标求导；临时梯度诊断恢复requires_grad。
5. LF标签损失不直接作用于HF校正输出；联合stage日志和实际参数更新一致。
6. 硬IC与t=0观测矛盾会被记录，不删除、不补值。
7. 所有研发入口拒绝test_Data标签，test power的真实温度不用于偏置估计/时间字典/检查点选择。
8. 最优、最后、阶段末检查点均真实保存且可加载，不通过修改metadata伪造来源。

新源码保留commit、dirty diff、untracked源码归档与有效命令；配置同时保存原始hash和规范化物理数值hash，历史缺失补丁不能事后伪造。

---

## 10. 可直接复制给Codex的下一条任务

> 按V5先完成M2-E、M2-L、M2-O，不运行E1/E2，不增加网络模块，不读取test_Data温度。保留B0和全部现有划分。重点核查硬水冷温度覆写是否把边界自动微分热流置零，采用分材料2πr确定性求积、解析控制场和参考FE口径复核能量预算；发布每项原始瓦数、求积收敛及散度恒等式。核对五份LF来源及Cu/SiC验证误差，区分实际LF拟合不足与检查点/评价口径错误。核查257 W热环t=0但不得未经证据改初温、删点或加偏置。完成后生成M2报告、LF质量表与是否允许进入M3的明确判定。只有通过审计后才对seed0实施相同数据和物理预算的排程对照，再试受限解冻，并保存各阶段最佳、阶段末和最终检查点。始终让仿真监督LF输出，Top/Hot/Cold与物理约束监督最终HF场。

## 11. 参考依据

本轮新增数字取自用户提供的M0/M1报告，不是独立复算的检查点结果。上述优先级、候选步频、求积阶数和验收门槛属于本项目建议，不是文献保证。

- 当前公开源代码版本：https://github.com/fei4440273/temperature-field-prediction/tree/bbe563e05ee65e28362ccab04d91773896d8d0c4
- 水冷覆写代码：https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/src/sic_cu/models/multifidelity.py
- 热源与辐射定义：https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/src/sic_cu/physics/boundary.py
- 轴对称PDE代码：https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/src/sic_cu/physics/heat_equation.py
- COMSOL官方，Global Variables for Heat and Energy Balance：https://doc.comsol.com/6.4/doc/com.comsol.help.heat/heat_ug_modeling.06.04.html 。用于热预算项定义，不表示用户必须改用COMSOL。
- Wang, Teng, Perdikaris (2021), Understanding and Mitigating Gradient Flow Pathologies in Physics-Informed Neural Networks，DOI:10.1137/20M1318043；作者版本：https://arxiv.org/abs/2001.04536 。支持梯度诊断与平衡这一机制，不把其论文增益套用到本项目。
