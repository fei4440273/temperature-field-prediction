# SiC–Cu 温度场预测项目完善计划（V2：高低保真联合建模主线）

> **用途**：交给 Codex，在现有工程上实施完善，不重建项目，不重新划分数据。
>
> **修订日期**：2026-09-08
> **版本**：V2，替代上一版完善计划；保留既定划分与工程框架，明确仿真数据为必需训练信息。
> **实施状态**：计划已修订；本次未修改远程仓库、未训练模型、未重新验证原始 CSV。
> **审查日期**：2026-09-08
> **审查仓库**：`fei4440273/temperature-field-prediction`
> **审查提交**：`adcf37f1082a53e5bc1522e6c0eabdc8d05b3969`
> **必须保留**：固定训练/验证/测试协议、多保真数据融合、SiC–Cu 分材料传热、现有预测导出接口。
>
> **本次审查范围**：已阅读当前仓库的协议、审计报告、几何/边界/训练配置，以及多保真模型、训练器、表面引导器、物理损失和部分评估代码。当前 Git 提交没有包含 `data/` 原始文件和训练检查点，不能据此声称已经重新运行全部测试、审计全部原始 CSV 或复现训练指标。下文将“代码可确认的事实”“仓库报告的历史结果”“待实施/待验证事项”分开。

---


## 0. 执行摘要：先构建完整低保真场，再用高保真校正同一个场

**本项目必须使用低保真仿真 + 高保真实验联合建模。不能将仿真降为只用于画对比图的参考，不能把仅使用实验拟合的表面模型当作最终预测方案。**

保留现有 DeepONet 低保真体场与多保真 PINN。先建立可追溯的多保真基线，再在同一数据协议下检验 PRC 的幅值—时间尺度校正。PRC 是校正机制候选，不是取消仿真或另做纯实验模型的理由。

```text
Simulation train：60 个功率，SiC + Cu 全域、完整瞬态
          │ 训练低保真分支 F_L；simulation validation 选择检查点
          ▼
低保真温度场 T_L(r,z,t,P,m)
          │ 以预测值/响应模式保留在最终模型内
          ├──────────────────────────┐
          ▼                          │
高保真校正器 C_H                    │
          ▲                          │
Experiment train：12 功率 Top/Hot/Cold │
          └───────────┬──────────────┘
                      ▼
        最终校正场 T_H = T_L + ΔT
                      │
     对 T_H 施加 SiC/Cu PDE、初值、边界、界面约束
                      │
          Experiment validation：3 功率选模型
                      │
  可选联合微调：仿真监督 LF 分支；实验监督 HF 输出
                      │
          冻结配置与检查点，再执行两套测试
                      ▼
 simulation test：低保真全场精度；HF test：Top/Hot/Cold 实测精度
```

### 0.1 三类信息分别提供什么

| 信息 | 覆盖范围 | 对应训练对象 | 不能误解为 |
|---|---|---|---|
| 低保真仿真 | SiC/Cu 体积域、时间、功率 | 完整低保真场、响应模式、空间结构 | 真实内部温度场实验标签 |
| 高保真 Top | SiC 顶面 | 最终场的表面偏差校正 | 全部内部场真值 |
| 高保真 Hot/Cold | Cu 底面两个半径 | 最终场在环位置的绝对温度/温升校正 | 成百上千个独立角向测点 |
| 物理方程 | 材料域及边界 | 最终校正场的物理一致性 | 实验数据或仿真数据的替代品 |

### 0.2 本次修订明确更正的重点

1. 低保真预训练必须使用两种材料的体积域数据，不能只提取仿真顶面后丢弃内部信息。
2. 每个正式多保真模型都必须能追溯到 simulation train，并在前向计算中保留低保真预测或其已学习的响应参数。
3. PRC 先完成全域低保真响应拟合，再接受 Top/Hot/Cold 校正。单纯表面拟合仅可作为模块诊断，不再是主开发路线。
4. 仿真标签主要监督 LF 输出，实验标签监督 HF 输出；不能默认强迫 HF 输出处处等于有偏差的仿真。
5. 保留 60/10/10 低保真和 12/3/3 高保真固定划分；不恢复留一功率/循环交叉验证。

此处的“高保真预测”指实验校正后的模型估计，并不意味着有完整三维实验真值。

---

## 1. 已经完成的内容与这次真正需要补齐的内容

### 1.1 可以保留，不需要从零开发

依据当前 README、配置和已审查代码 [R1–R8]：

| 内容 | 当前证据 | 本计划处理 |
|---|---|---|
| 功率级数据划分 | `configs/splits.yaml`、训练器有固定集合检查 | 保留，增加产物来源校验 |
| 原始数据审计、Parquet 预处理 | 审计报告记载 543 个 CSV | 本地重跑核验，不重写解析器 |
| SiC/Cu 材料标签、统一物理损失 | 已有配置和实现 | 保留，补导数/边界回归测试 |
| MLP、LSTM、POD、DeepONet 候选 | 已有实现与历史报告 | 复用，不再全部重新研究 |
| GNO | 当前明确仅允许数据驱动路径 | 保持，不将其结果标成 GNO-PINN |
| 普通多保真残差、硬表面引导 | 已有实现 | 作为不同基线配置，不混成同一方法 |
| 任意功率与 NPZ/CSV/VTK/PNG 输出 | README 及脚本已列出 | 扩展检查点适配，不重做可视化体系 |
| Docker/DDP | 仓库报告双 A40 已做冒烟 | 沿用环境；本次未独立验证硬件 |

### 1.2 需要先解决的六件事

1. **数据和训练产物在本地存在，但没有随当前 Git 提交提供。** `.gitignore` 排除了 `/data/`、检查点与大部分 JSON/CSV 报告。Codex 必须在用户本机挂载真实数据运行，不能用旧数据仓库或合成数据假冒新数据。[R4]
2. **旧协议结果不能代表当前划分。** `PROJECT_PROGRESS.md` 顶部已注明旧结果须按新协议重训；其正文仍保留旧工况、旧折数、旧指标。[R3]
3. **硬表面引导锁定了顶面输出。** 在 SiC 的 `z=0`、引导有效区间内，内部修正网络对顶面预测不起作用。单纯扩大内部 MLP，不能解决顶面引导器自身的误差。[R7]
4. **训练、选择检查点和最终报告的聚合口径需要统一。** 训练器的观测损失/传感器验证仍有按记录数聚合的路径，最终 IR 评估则已有按功率汇总。不同长度的工况可能获得不同权重。[R6、R10]
5. **表面缺失末段存在“保持最后一帧”的建模假设。** `ResidualTrajectory.evaluate` 对超过观测末时刻的查询使用末帧；它可以是显式先验，但不能伪装成真实观测。[R8]
6. **物理参数状态需要统一解析。** 配置中的 `verified: false`、发射率空值和“名义值/有效初值”并存，不能简单将 `verified` 改为 `true` 跳过检查。[R5]

本计划将这些事项视为精度与可信度的共同前置工作，而不是继续堆新网络。

---

## 2. 数据协议：完全保留现有划分

### 2.1 高保真：12 / 3 / 3 个完整功率

以下内容来自现有 `configs/splits.yaml` 和 `DATA_USAGE_PLAN.md`，不得擅自改变。[R2]

| 集合 | 功率 / W | 允许用途 |
|---|---|---|
| 训练集 | 55、216.8、254.5、257、309、364.3、430、494.2、532、558.5、593.5、729 | 数据损失、参数拟合、梯度更新 |
| 验证集 | 115.2、403、630.5 | 早停、模型/超参数/检查点选择 |
| 测试集 | 169、339、634 | 全部决策冻结后的最终评价 |

同一功率的 Top、Hot、Cold 必须保持同一角色。测试集不得用于训练表面引导器、校正器、物理参数或不确定性尺度。

当前 `external_sensor_test` 配置为空。**不要恢复旧方案中 50/250/350/500/600/700/800 W 的外部测试划分。** 新协议以本节为准。

### 2.2 低保真：保持 60 / 10 / 10

```yaml
simulation_validation_w: [90, 170, 250, 330, 410, 490, 570, 650, 730, 800]
simulation_test_w: [50, 130, 210, 290, 370, 450, 530, 610, 690, 770]
simulation_train_w: 10 到 800、步长 10，扣除上述两组后的 60 个功率
```

低保真模型只拟合 simulation train；simulation validation 只选模型；simulation test 同样不能在研发循环中反复查看。

这里是**按保真度分别定义的两个评价任务**。低保真模拟与高保真实验不是同一类标签；最终测试中的“未见功率”必须说明是未见实验工况还是未见模拟工况。当前高保真测试功率不是 10 W 仿真网格上的功率，仍需记录这一事实，而不是笼统宣传所有数据源完全同分布且统计独立。

### 2.3 不重划分，但要检查三种不同的“独立”

- **集合独立**：文件/功率集合不相交；现有配置满足这一形式要求。
- **计算过程隔离**：归一化、POD 基、响应模式、引导器、物理参数、模型选择没有使用测试标签；需要代码与产物来源共同证明。
- **采集与研究过程独立**：是否不同试验批次、是否看过测试结果，不能只凭文件夹名字证明。

仓库协议已披露：169/339/634 W 曾在旧流程中作为验证数据被查看。[R2] 因此继续将它们隔离为固定测试集是合理的，但论文应称为**固定留出测试**，不能声称此前从未查看。此限制不通过重新命名文件或重新分组“消除”。

若未来有新的独立实验，可以在模型冻结后作为额外前瞻性检验；这不是本计划启动的前置条件，也不改变当前三个集合。

### 2.4 测试集封存的具体要求

建议新增 `configs/release_manifest.yaml`，记录：

```yaml
protocol_id: hf_fixed_12_3_3_v4
split_sha256: REQUIRED
raw_manifest_sha256: REQUIRED
processed_manifest_sha256: REQUIRED
code_commit: REQUIRED
physics_config_sha256: REQUIRED
selection_metric_version: macro_v1
frozen_model_configs: REQUIRED
frozen_seeds: [0, 1, 2, 3, 4]
frozen_checkpoint_sha256s: REQUIRED
test_access_purpose: final_evaluation_only
historically_exposed_test: true
```

新增文件和字段是**待实施要求**，不是仓库已经具备的功能。

训练入口不输出 test 指标；最终测试单独调用。一次冻结测试可以批量评价预先登记的多个基线、消融和 5 个种子，但不能看完测试后再决定删哪个模型、选哪个种子或换超参数。

---

## 3. 阶段 A：工程与数据来源补齐

### A1. 在用户本机确认真实资源

保留现有目录与大小写，包括原始拼写：

```text
data/Simulation_data/
data/Experiment_data/Topdata/
data/Experiment_data/HotData/
data/Experiment_data/ColdData/
data/test_Data/topdata/
data/test_Data/hotdota/
data/test_Data/colddata/
```

当前 Git 仓库没有这些原始数据，不表示用户本地没有。Codex 的任务是检查本地路径与清单，不是重新向用户索要所有已确认参数。

验收：生成 `reports/current_protocol/data_inventory.json`，逐文件包含相对路径、大小、SHA-256、来源、功率、模态、split。训练进程的清单不应包含测试温度的统计结果；全局来源审计可检查测试文件的存在、格式、重复性，但不能据测试分布选择模型或清洗阈值。

### A2. 保留预处理，增加可复现指纹

审计报告的数量基准为：80 个仿真文件、365 个开发集 IR 文件、30 个开发集环温文件、62 个测试 IR 文件、6 个测试环温文件，总计 543 个。[R9]

本地实物不一致时：停止依赖数据的训练，输出差异；不要直接改计划数字使检查通过。

新增/补齐：

- 原始文件到 Parquet 的来源映射；
- 功率键沿用现有小数容差机制；
- 缓存键包含 split、源文件哈希、预处理配置、代码版本；
- 旧 processed/cache 不匹配时重建，不能沿用旧 split 列；
- Top/Hot/Cold 共用 run_id 或可靠的采集映射；缺失时标为未知；
- 相同哈希、同一原图的重命名/重标功率检查。近邻功率本身不是泄漏证据，不应自动删除。

### A3. 不把“修复/重建标记”当作实验噪声真值

现有 IR 数据已经被确认是项目使用的最终温度场。保留径向统计与 `is_recovered` 元数据，不再要求重新做相机原始辐射标定。[R1、R9]

环内标准差可以用作轴对称一致性/稳健权重的线索，但不等于经过标定的传感器噪声方差。初版不声称据此得到严格概率置信区间。

同一时间重复的 148/218 个环点继续压缩为一条环温观测，不能增加独立样本量。

### A4. 已确认的物理条件直接沿用

从当前配置读取而不是再次猜测：[R5、R11]

- `z=0` 是顶面，SiC 底面 `z=-0.012 m`，Cu 底面 `z=-0.0175 m`；**不能套用旧方案的正向深度坐标**。
- 初始、环境和外圆柱水冷温度对外统一记为 `22℃`；物理方程内部转换为 `295.15 K`。
- 激光为恒定高斯输入，吸收率配置 `0.8`，光束 `1/e²` 强度半径 `0.02 m`。
- Cu 外圆柱 `r=0.05834 m` 是定温水冷面，不是整个底面定温。
- 顶部/底部有效自然对流系数配置分别为 `9.0`、`4.3 W/(m²·K)`。
- 热物性从 `configs/materials.yaml` 读取。
- 接触热阻 `7.340724e-5 m²·K/W` 是现有低保真有效初值，不自动宣称为真实唯一热阻。
- 发射率 `0.5/0.5` 是已采用的名义情景，不自动宣称为材料测量值。

新增 `resolved_physics.yaml` 作为每次运行的解析后快照，区分 `measured/confirmed/effective_initialization/assumed_scenario/learned_on_train`。不得只修改 `verified: true` 绕过检查。

物理参数先在统一名义情景下比较算法；只有模型结构锁定后，再做小范围敏感性检查，避免每个模型使用不同物理参数而失去可比性。

---

## 4. 阶段 B：先统一预测接口、评价口径和导数检查

### B1. 连续坐标网络优先在真实观测位置评价

当前 `eval/high_fidelity.py` 的一条路径先在预测网格生成温度，再以 `np.interp` 回插到 IR 径向位置。[R10] 对连续坐标网络，这会额外引入网格插值误差。

新增或统一接口（以下为目标接口）：

```python
predict_points(coordinates)  # [N,5] = [r_m, z_m, time_s, power_w, material_id]
# 内部返回 [N,1]，单位 K；报告与导出层转换为 ℃
```

要求：

- MLP、DeepONet、普通校正和 PRC 直接在 IR/ring 坐标调用；
- 网格型方法、FEM 插值使用其明确的空间插值器，并单独报告插值误差；
- 3D 渲染网格不能决定科研评价分辨率；
- 比较直接查询与旧路径的差异，不能把评价器变化误写成算法增益；
- 对固定模型，改变可视化网格不应改变直接坐标指标。

### B2. 统一等权工况评价

高保真主要评价单位是**完整功率工况**，不是像素数。

对每个功率 p 的 IR：

\[
E_{p}^{IR}=\sqrt{\frac{1}{N_{t,p}}\sum_t\sum_i w_{p,t,i}(\widehat T_{p,t,i}-T_{p,t,i})^2},\quad\sum_i w_{p,t,i}=1.
\]

再计算：

\[
E^{IR}_{macro}=\frac{1}{N_P}\sum_p E_p^{IR}.
\]

这里是“逐功率 RMSE 的算术平均”。另一个“等权功率 MSE 开根号”的统计量可以保留，但必须用不同名字，不得互换。

Hot/Cold：先对每条曲线分别算误差，再对热/冷端和功率等权平均，不让长序列天然占更大比例。

建议保持现有三类选择权重 `1.0/0.2/1.0`，但采用统一宏平均输入：

\[
S_{val}=\frac{E^{IR}_{macro}+0.2 E^{ring,abs}_{macro}+E^{ring,\Delta}_{macro}}{2.2}.
\]

权重在本轮实验开始前冻结；这是工程选择指标，不是物理定律。记录 `selection_metric_version=macro_v1`，旧口径结果只能作为历史参考。

### B3. 主指标与辅助指标分开

| 层级 | 必须报告 |
|---|---|
| 验证与测试主指标 | IR、Hot、Cold 分别的 MAE、RMSE、最大绝对误差；综合 S |
| 时间诊断 | [0,30)、[30,100)、[100,200] s，按真实可用时刻计算 |
| IR 两种口径 | 径向均值误差、原始二维像素回投误差，两者不能混报 |
| 功率诊断 | 三个验证/测试功率逐个列出，不只给一个总平均 |
| 低保真完整场 | SiC/Cu 分别误差、全场误差、峰值温度误差 |
| 物理检查 | 独立配点上的 PDE/BC/IC/界面残差、外圆温度和梯度连续性检查 |
| 计算成本 | 参数量、训练时长、峰值显存、相同输出规模下推理时间 |

区间没有观测时输出 N/A，禁止补成零误差。IR 首帧 5 s 不能用于声称 0–5 s 有实验验证。

温升主参考保留为当前曲线第一观测时刻 `T(t)-T(t_first)`，并清楚标明不等同 `T(t)-T(0)`。绝对温度误差始终保留，防止用基线相减隐藏绝对偏差。

当前一个评估路径以摄氏峰值作相对误差分母。[R10] 为避免温标零点影响，新增**峰值绝对误差 ℃**作为主工程量；相对指标使用预先定义的峰值温升分母及小温升阈值，旧摄氏相对值标为 legacy，不直接比较。

### B4. 轴对称误差下限

保留原始二维 IR 的轴对称投影误差。对固定环分箱与平方损失，环均值是该箱常数预测的最小二乘解，可估计当前轴对称模型无法解释的周向差异。

这不是全模型可达精度的保证，也不能直接从 RMSE 中相减得到“纯网络误差”。在分箱、权重一致的条件下，使用 MSE 分解，保留离散化说明。

### B5. 双卡评估一致性

当前验证加载器使用 `DistributedSampler`。[R6] PyTorch 官方文档说明不可整除时，默认 sampler 可能补索引。[E2]

因此：

- 简单方案：rank 0 使用非 DDP 模型完整评价验证集，再广播结果；
- 或按唯一观测 ID 聚合并去重后统计；
- 不直接平均各卡 RMSE；聚合误差和/权重，或聚合完整逐工况指标；
- 单卡和双卡在相同检查点上应在预定数值容差内一致。

### B6. 物理自动微分与边界测试

复用现有物理损失，但增加以下回归检查：[R7、R12]

- 一阶/二阶空间导数和时间导数与解析/中心差分小算例一致；
- 计算物理残差时不能在 `no_grad` 下调用模型；冻结参数不等于截断输入坐标梯度；
- 自动微分对每个点的含义明确，GNO 节点耦合不能套用独立点 Jacobian；
- 顶面外法向在当前负 z 几何中为正 z；激光是入射热流，不能改变当前符号约定；
- 轴线 r=0 使用正确极限或轴对称光滑表示，而不只靠很小的除数；
- `torch.where(isclose(r,R), T_cool, T_pred)` 是边界值覆写，不保证内侧极限连续。此路径目前存在于模型中；是否被各配置启用需检查。用 r=R−ε 到 R 的单侧逼近检验，而不是只查 r=R 温度。[R7]
- 对新增模型优先采用连续的试探函数约束，或保留清晰的边界损失；不要用导出后裁剪温度来掩盖边界/PDE 错误；
- 不在 t=0 同时硬压不相容的初值与骤然开启的热流；初始—边界角点单独处理。

可选整体能量诊断使用轴对称体积权重 `2πr`，需要适当网格积分权重；无可信积分权重时不得用简单节点均值假装能量守恒。

---

## 5. 阶段 C：在当前固定协议下重建可比较基线

### C1. 只保留必要对照

| 对照 | 作用 |
|---|---|
| FEM 功率线性插值 | 保留低保真工程下限/强对照，不宣称它是高保真实验模型 |
| MLP-PINN + 普通多保真校正 | 最基本的物理与多保真基线 |
| DeepONet-PINN + 普通多保真校正 | 判断算子表示和普通温度残差的能力 |
| 现有 DeepONet LF + 硬表面引导 + 内部 PINN | 历史强候选，在新划分下必须重新拟合引导器与校正器 |
| PRC-PINN | 新增的受控候选，仅在先导实验通过后做完整训练 |

以上“多保真”“跨材料”属于所有方案的共同条件，不添加到方法简称。

LSTM/POD 已有代码和历史结果可以保留，不需要删除；本轮不重新扩大调参预算。GNO 物理导数尚未有效实现前不进入正式 PINN 对照。FNO、完整 LNO/PILNO、扩散模型不属于本轮交付范围。

### C2. 哪些检查点必须重训

- 高保真训练功率包含当前验证功率，或来自旧全数据/旧 LOGO 训练的检查点：不得用于正式对照。
- 表面引导器、POD/响应基、归一化等产物未记录来源：先判为来源待核实，不能仅修改元数据“迁移”。
- 低保真检查点若确实只使用当前相同的 simulation train、预处理和物理配置，可以复用，但必须能验证清单与哈希；否则重训。
- README 中的旧部署路径不再作为默认推荐模型，直到新的 release 文件明确指向当前合规产物。

代码已检查低保真检查点中的训练功率清单；本计划在此基础上增加源数据与配置指纹，不声称现有检查完全缺失。[R6]


### C3. 必须明确区分的低保真、高保真输出

令 X=(r,z,t,P,m)，m 为材料标签；Y_L 为原始仿真温度。

低保真分支：

\[
\widehat T_L(X)=F_L(X;\theta_L).
\]

高保真校正分支：

\[
\widehat T_H(X)=\widehat T_L(X)+C_H\big(X,\widehat T_L(X);\theta_H\big).
\]

现有 `AdditiveCorrectionModel` 已经具备低保真输出与温度条件化校正接口，应复用而不是重写。[R7]

仿真和实验的功率、节点、时刻不要求一一重合。校正器在实际实验坐标上查询 LF 网络；同一监督对内部必须使用相同 P、r、z、t、材料标签及单位。不能将相邻仿真功率直接冒充实验功率，也不能要求先运行一个新的 FEM 工况才能推理。

### C4. 三阶段训练：仿真在每阶段的真实作用

| 阶段 | 原始仿真如何使用 | 实验如何使用 | 更新对象 |
|---|---|---|---|
| A：低保真预训练 | 从 60 个训练功率采样 SiC/Cu 全域真实仿真记录 | 不使用 | LF 分支 |
| B：高保真校正 | 保留并调用已预训练 LF 分支；仿真知识没有被删除 | 12 功率 Top/Hot/Cold | 校正器；物理参数按统一冻结规则 |
| C：可选联合微调 | 每步或固定步频回放 simulation train，监督 LF 分支 | 同时监督 HF 输出 | 声明解冻的 LF 参数 + 校正器 |

A 使用 simulation validation 选择检查点；B/C 使用高保真 validation 选择最终预测模型，并监测 LF 验证精度是否退化。测试集不参与这些决定。

B 冻结 LF 参数不等于删除低保真信息，也不等于可以 `detach()` 或 `torch.no_grad()` 整个 LF 前向：最终场计算 PDE 时仍需要 LF 对 r、z、t 的导数。冻结参数用 `requires_grad_(False)`，保留坐标计算图。

C 不是必须开启。保持 LF 冻结的校正模型也属于多保真。只有验证证据支持，才解冻联合微调；重建 optimizer 时要核对实际 `requires_grad` 和梯度，不能仅把参数传入 optimizer 就宣称已解冻。

### C5. 联合损失：仿真监督 LF，实验监督 HF

定义：

\[
L_L=\operatorname{MSE}_{X\in D_L^{train}}\left[(\widehat T_L(X)-Y_L(X))/S_T\right].
\]

高保真观测损失：

\[
L_H=\lambda_{top}L_{top}(\mathcal H_{top}\widehat T_H,Y_{top})
+\lambda_{hot}L_{hot}(\mathcal H_{hot}\widehat T_H,Y_{hot})
+\lambda_{cold}L_{cold}(\mathcal H_{cold}\widehat T_H,Y_{cold}).
\]

观测算子分别是 SiC 顶面查询和 Cu 底部两个环的位置查询。轴对称且环温均匀时，每个环在每时刻只提供一个观测。需要同时提供绝对温度误差与相对于各自真实首个观测时刻的温升误差；不得把 t=1 s 强行改名为 t=0。

物理损失：

\[
L_{phy}=L_{PDE,s}(\widehat T_H)+L_{PDE,c}(\widehat T_H)
+L_{BC}(\widehat T_H)+L_{IC}(\widehat T_H)+L_{interface}(\widehat T_H).
\]

联合阶段：

\[
L=\lambda_L L_L+L_H+\lambda_{phy}L_{phy}+\lambda_{reg}L_{reg}.
\]

`L_reg` 对小规模校正幅值、时间尺度变化或功率方向平滑施加温和先验，按验证选择，并明确它会限制校正能力。不能把先验伪装成未观测的高保真标签。

现有 `train/multifidelity.py` 的联合阶段从 `splits.simulation_train` 读取仿真，但用 `model(lf_coordinates)` 即 HF 输出计算仿真 MSE。[R6] 这是一个可研究的 LF 回拉正则，不是天然代码错误；然而它与希望纠正仿真偏差的目标可能冲突。因此：

- 本次新主路径默认将仿真数据损失改到 **LF 分支输出**；
- 原 HF 回拉形式保留为清楚命名的对照，不静默保留为强监督；
- HF 体积域没有实验标签的地方依靠 LF 先验与真实物理约束推断，不把 LF 当作 HF 真值；
- 不能通过简单把 `low_fidelity` 权重设为零来完成“修复”，那会移除联合阶段的仿真锚定。

### C6. 实施接口、双数据流与日志

建议扩展前向接口（拟新增，不是声称当前已实现）：

```python
pred_lf = model(x_lf, fidelity="low")
pred_hf = model(x_hf, fidelity="high")
```

LF/HF 分支在单卡、DDP 中保持同一数学计算；训练时不要绕过 DDP wrapper 调用内部子模块做可训练前向而不检查同步。可改为一次前向同时返回所需输出，具体由实现测试决定。

训练语义伪代码：

```text
预训练：simulation_train_batch -> LF预测 -> LF数据损失 -> 更新LF
校正：HF_train_batch -> LF预测 + 校正 -> HF数据损失 + 最终场物理损失
联合：simulation_train_batch -> LF数据损失
      HF_train_batch -> HF数据损失
      物理点 -> 最终HF场物理损失
      汇总 -> 更新声明解冻的参数
```

每个运行至少记录：`lf_checkpoint_hash`、LF/HF 训练功率、实际消费 LF/HF 点数、LF 数据损失、Top/Hot/Cold 损失、PDE/BC/IC/interface 损失、LF/HF 参数梯度范数、冻结/解冻状态。`joint` 阶段若声明启用 LF 回放但实际消费 LF 点数为零，应失败。

### C7. 数据采样改为工况、材料、时间平衡

仿真：先等概率抽训练功率，再抽 SiC/Cu 材料、时间段和空间点。每个完整训练周期都须覆盖两种材料；不要让高温 SiC 或节点较多的 Cu 单独主导。训练平衡权重与真实体积积分权重是不同概念，分别记录。

实验：先抽训练功率，再抽 Top/Hot/Cold，之后抽帧/时刻及径向点。IR 像素数量不等于独立工况数量。

所有损失先在本模态归一化、再按预定权重组合，不能靠“仿真行数远大于实验行数”决定权重。沿用全局真实时间尺度，不按每功率稳态时刻将时间压缩到 0–1。

代码当前每轮有多次数据优化、另有物理优化。[R6] 记录数据/物理步数与梯度范数，设置固定、可复现步频；本轮不同时叠加多种动态权重和自适应配点。

### C8. 核心多保真对照

正式主方案始终使用 LF+HF。为证明仿真和实验各自的价值，增加受控诊断：

| 变体 | 使用信息 | 可回答的问题 |
|---|---|---|
| LF-only | simulation train | 只有仿真先验时与实测差多少 |
| HF-only（仅消融） | HF train + physics，无仿真预训练/引导/蒸馏 | 去掉仿真后数据效率与泛化如何 |
| LF+HF，无物理（仅消融） | 两类训练数据 | 物理约束是否有实际收益 |
| LF+HF+physics | 完整方案 | 最终多保真目标 |

HF-only 不是项目替代方案。它仅用于验证仿真贡献，不能作为“已完成多保真”的交付。所有方案按相同验证协议和相近预算比较，不预设哪个必定胜出。

---


## 6. 阶段 D：PRC 候选必须从全域仿真学习基础响应

### D1. 方法定位

PRC-PINN（极点—留数校正物理信息神经网络）是项目候选名称，不是已经发表算法的正式名称。A 是阶跃响应的模态幅值，与传递函数留数相关但并不相同。LNO 已有极点—留数表示的文献，本计划不主张首次提出该表示。[E1]

候选创新是：在多保真框架内，将仿真—实验差异从自由温度残差转为幅值和响应时间尺度校正。名称不附加 MF/CMT；多保真和跨材料属于共同研究条件。

### D2. 为什么不能仅替换一个纯实验表面引导器

现有硬绝对温度引导在 SiC 顶面、引导权重为 1 时，基础表达会发生：

\[
T_s=T_{LF,s}+(G-T_{LF,s})=G.
\]

这发生在后续温度下限/初值投影之前；投影后顶面仍只依赖 G 和固定约束，不再直接依赖 LF 输出。[R7] 这不等于整个模型从未使用仿真，但不能把该表面精度全部归因于当前神经网络的多保真融合。

新主路径要求：HF 校正依赖从真实 simulation train 学到的 LF 场或 LF 响应参数；不把仅由实验拟合的绝对 G 写入表达式以消去 LF。硬引导历史方案保留为强对照，不删除。

仅做表面时间表示拟合可以是廉价诊断，但必须标注诊断性质；不得绕过全域 LF 预训练，也不得把诊断输出当作最终三维多保真结果。

### D3. 全域 LF 响应模型

SiC/Cu 分别具有空间幅值，共享耦合系统响应时间尺度：

\[
\widehat T_{L,m}(r,z,t,P)=T_0+\frac{P}{P_{ref}}C_m(r)
\sum_{j=1}^{K}A^L_{m,j}(r,z,P)\left[1-e^{-t/\tau_j^L(P)}\right].
\]

在当前固定外圆温度几何下，可测试 `C_SiC=1`、`C_Cu=1-(r/R_Cu)^2`，并验证它与真实边界及物理导数一致。沿用 z∈[-0.0175,0]；不改成旧讨论中的正向深度坐标。

原始仿真给出的是真实完整时空温度标签，**不是 A 或 tau 标签**。A 和 tau 通过重建所有训练仿真快照来学习，不要求人为给每个模式标注物理含义。

先用原始 `simulation train` 训练 LF 响应表示，simulation validation 选 K 和宽度。即使为降低计算量只抽样部分节点/时刻，也须包含 SiC 和 Cu 体积域、早期与后期响应，不能只用顶面。

已存在 DeepONet LF 检查点可作为暖启动教师/辅助蒸馏，但它必须只来自相同 simulation train。真实仿真监督不能被来源不明的教师完全替代，蒸馏误差单独报告。不要求 PRC 必须在前向里再套一层完整 DeepONet；普通 DeepONet 基线与 PRC 可以使用不同表示，但必须使用同一低保真数据来源和预算。

实现要求：

- 初试 K=8/16；验证确认欠拟合再扩展 K=32；
- 空间网络使用平滑激活、适当的 r² 表示，支持二阶自动微分；
- A 允许有符号；tau 使用正增量累加保证正值与有序；
- `-torch.expm1(-t/tau)` 计算趋稳核，避免小 t 的消减误差；
- 时间尺度初版仅依赖 P，不依赖 r/z/t；
- 在本项目固定平衡温度和零输入前提下，t=0、P=0 输出 T0；
- 这些模式是近似表示，不声称是唯一真实热模态。

### D4. 用高保真实验校正同一 LF 响应

保持 LF 响应参数，再学习：

\[
A^H_{m,j}=A^L_{m,j}+\delta A_{m,j},
\quad d_j^H=d_j^L\exp(s_{max}\tanh b_j(P)),
\quad \tau_j^H=\tau_{min}+\sum_{l\le j}d_l^H.
\]

\[
\widehat T_{H,m}=T_0+\frac{P}{P_{ref}}C_m(r)
\sum_j (A^L_{m,j}+\delta A_{m,j})\left[1-e^{-t/\tau_j^H(P)}\right].
\]

`delta A=0`、`b=0` 时必须严格回到 LF 响应（使用相同数值路径）。校正器可以接收 LF 幅值或 LF 温度特征，但输入的梯度链必须保留。

高保真损失同时来自 **12 个训练功率的 Top + Hot + Cold**，不是仅拟合 Top。没有实测内部标签时不伪造 HF 全场。

### D5. 同一 LF 主干上的三变体

| 变体 | 校正内容 | 必须共享的条件 |
|---|---|---|
| V | 普通小规模温度残差 | 同一 LF 响应检查点、LF/HF 训练来源、物理条件 |
| A | 仅模态幅值校正 | 同上，tau 固定 |
| AT | 幅值 + 时间尺度校正 | 同上，加入受控 tau 校正 |

同时与原有 DeepONet 普通多保真模型和硬引导强基线比较。不能只超过自建弱对照就宣称有创新价值。

### D6. 缺失时间与开发门槛

只在真实观测时刻计算 HF 数据损失。最后实测帧不自动等于稳态，观测末段以后是预测，不补成实验标签。教师生成数据独立标识来源和权重，不能读取验证/测试温度生成训练标签。

先导实验的第一关是 **全域 LF 重建**：查看 simulation validation 的 SiC、Cu、早期瞬态与后期误差，确认时间表示本身不是瓶颈。门槛在运行前登记，达不到则先修 LF 或停止 PRC，不拿少量 HF 强行补救不合格 LF。

第二关才是 **高保真校正增益**：AT 对比 V/A，按相同 Top/Hot/Cold 综合指标与物理检查选择。固定 seed=0 小范围筛选，最多保留两个配置跑 seeds 0–4。可预登记综合误差下降 5%、不少于 4/5 个种子改善的研发门槛，但这不是统计显著性证明。模态退化容忍度也须预先写入配置。

---


## 7. 阶段 E：联合微调与全域物理约束

### E1. 复用工程，不重写数据与物理模块

新增 `src/sic_cu/models/prc.py`，适配现有 `[r,z,t,power,material_id] -> temperature_k` 接口，以及 LF/HF 双输出。普通 DeepONet 多保真与 PRC 多保真共享加载器、数据协议、物理损失、直接坐标评价和导出接口。

PRC 新检查点使用独立、可识别的 method，例如 `prc_multifidelity`；不得把新 state_dict 冒充旧模型。冻结 LF 后仍对最终 T_H 计算 SiC/Cu PDE、IC/BC/interface，不只约束 LF。

### E2. 可选联合训练的损失作用对象

沿用 C5：LF 数据 MSE 监督 T_L，Top/Hot/Cold 监督 T_H，物理损失监督 T_H。对 PRC，LF 仿真锚定的是 `A_L/tau_L` 组合得到的基础场；HF 学的是 `delta A/b`。联合解冻时两条数据流同时有效，不是丢掉 LF 后任意微调。

默认保留已确认热物性、光斑、吸收率、水冷边界、名义辐射率与有效热阻的统一配置。不能通过同时反演许多物理量掩盖模型表示不足。

### E3. 数值与物理检查

在 A、tau 不显式依赖 t 时：

\[
\partial_tT_H=\frac{P}{P_{ref}}C_m(r)
\sum_j\frac{A^H_{m,j}}{\tau_j^H}e^{-t/\tau_j^H}.
\]

使用解析时间导数核对自动微分；空间导数包含网络、r²、归一化、Cu 包络的链式法则。共享时间尺度不等于两材料同温，材料分支仍独立取值并施加正确的界面物理。

正 tau 只避免该响应表达时间指数发散，不自动保证能量守恒、最大值原理或真实精度。所有物理诊断保留；不通过导出时裁剪/覆写掩盖错误。

### E4. 推理与评价明确分工

```text
推理输入：P、查询坐标和时间、固定物理配置
推理内部：LF分支或LF响应参数 -> HF校正 -> 最终T_H
推理不输入：当前功率的真实Top/Hot/Cold、当前功率的新FEM结果
输出：温度场、LF/HF来源、校正大小、模型与数据适用范围
```

例如 P=36 W：先由 LF 学到的 10–800 W 规律预测基础场，再由 HF 校正器推断偏差。36 W 低于当前 HF 训练最小 55 W，属于高保真校正外推；不能因 LF 覆盖而宣称相同实验可信度。

simulation test 用于评价 T_L 对原始仿真全场的泛化；固定 HF test 用于评价 T_H 对 Top/Hot/Cold 的预测。T_H 与仿真的内部差值是“校正差异”，不是有真实内部标签支持的“预测误差”。

新检查点须保存 LF 来源/训练清单、校正来源/训练清单、所有冻结参数、解析物理配置、LF/HF双输出的模型构造信息和协议指纹。不识别模型时失败，不能静默退回 FEM。

---

## 8. 独立测试如何实施与解释

### 8.1 测试前冻结

冻结以下内容后才允许执行测试：

- 最终比较模型/消融名单；
- 数据/代码/物理配置指纹；
- 模型结构、K、损失权重、训练步数策略；
- 5 个固定种子及各自验证最优检查点；
- 是否使用种子集成；
- 主次指标与聚合方式；
- 测试评估脚本版本。

多种子标准差表示优化重复性，不表示实验随机不确定性，也不能充当三个测试功率构成的总体置信区间。

### 8.2 最终测试内容

对 169、339、634 W 分别输出：

```text
IR:    径向指标、二维回投指标、峰值、早/中/晚时间段
Hot:   绝对温度与相对第一观测的温升曲线
Cold:  同上
Physics: 未参与优化的独立配点残差诊断
```

固定低保真测试功率评价完整场精度，但必须标为**仿真参考精度**。真实 SiC/Cu 内部场没有实验真值，不能给出“真实内部 RMSE”。

### 8.3 实验范围与时间支持

当前 HF 训练范围是 55–729 W；36 W 仍是相对实验训练范围的外推，虽然处于 LF 仿真功率区间内。[R2]

最终三维输出称为**轴对称三维温度场预测/物理约束推断**。系统需区分：

- 相对 LF 训练范围的插值/外推；
- 相对 HF 训练范围的插值/外推；
- 查询时刻是否有该类观测支持；
- 仿真支持的 0–200 s 与实验实际测量时长。

P=0 在给定背景条件下可以作为解析边界测试；P=800 的实验可信程度不能因为仿真目录有文件就默认很高。超出声明范围默认拒绝，显式允许外推时输出醒目标记。

### 8.4 测试不达标的处理

如测试表现不好，完整保留结果。不能继续以该测试集为反馈反复修模型后仍宣称它保持盲测。

可以继续研发，但该轮评价应明确记录为“测试结果已反馈研发”，需要新的外部验证证据才能支持新增泛化声明。

---

## 9. 可直接交给 Codex 的任务表

新增路径均为建议，不表示已经存在。优先修改现有模块，避免平行再造一个工程。

| 编号 | 优先级 | 任务与主要文件 | 验收产物 |
|---|---|---|---|
| P0-1 | 必须 | 本地数据/产物盘点；现有 audit/build | `data_inventory.json`、缺失资源清单 |
| P0-2 | 必须 | `data/splits.py`、缓存/检查点来源校验 | 固定 split、哈希检查测试 |
| P0-3 | 必须 | 解析物理配置、整理 README/进度文档 | `resolved_physics.yaml`、当前/历史状态分离 |
| P1-1 | 必须 | `eval/high_fidelity.py`、预测适配器 | 直接坐标评价与旧路径差异报告 |
| P1-2 | 必须 | `train/multifidelity.py`、`eval/metrics.py` | `macro_v1` 及单/双卡一致性测试 |
| P1-3 | 必须 | 物理损失/边界/模型导数检查 | 边界单侧连续、轴线、解析导数测试 |
| P2-1 | 必须 | simulation train 的全域 LF 预训练/来源核验，然后高保真校正 | LF 验证表、LF来源、合规多保真检查点 |
| P2-2 | 必须 | 双数据流、LF分支仿真监督、工况/材料平衡与回拉对照 | LF/HF实际消费日志、梯度与验证对比 |
| P3-1 | 候选 | 新增 `models/prc.py`，SiC/Cu 全域 LF 响应预训练 | K=8/16 的分材料 LF 验证结果 |
| P3-2 | 条件执行 | PRC 全域响应的 Top/Hot/Cold 高保真校正 | V/A/AT 全模态验证与物理检查 |
| P3-3 | 条件执行 | 双分支联合微调与信息源消融 | LF-only、HF-only诊断与完整多保真对照 |
| P4-1 | 必须 | 新增冻结发布清单 | 配置/检查点哈希、最终模型名单 |
| P4-2 | 必须 | 复用 `06_evaluate_test_data.py` | 三测试功率逐项、5 种子完整报告 |
| P5-1 | 必须 | 复用 `06_predict_power.py` | 任意功率预测与来源/适用范围说明 |

建议新增少量文件：

```text
configs/release_manifest.yaml
src/sic_cu/models/prc.py
src/sic_cu/eval/protocol_checks.py
src/sic_cu/data/balanced_sampler.py
scripts/03_train_prc.py
scripts/06_freeze_release.py
tests/test_protocol_provenance.py
tests/test_macro_metrics.py
tests/test_prc.py
reports/current_protocol/README.md
```

适配现有目录结构，模型主干仍在 `src/sic_cu/` 中。不新建一套无关联的 `src/models/`。

---

## 10. 必须补的自动测试

### 协议/来源

- 12/3/3 功率与现有配置严格相等，集合不相交；
- train loader 只含训练功率，validation loader 只含验证功率；
- 显式传功率参数不能绕过 split 过滤；
- 表面教师、POD/响应基、归一化只从训练标签拟合；
- 将验证/测试温度替换为任意哨兵值，训练数据与拟合预处理产物不能改变；验证得分可以改变，但训练梯度输入不能改变；
- 旧全数据/LOGO 检查点与缺少来源的缓存必须被拒绝；
- 缺少原始数据时明确报错，不静默生成合成数据替代；
- 合成测试只用于软件单元测试，结果不得进入科研表格。

### 评价

- 人工两功率短/长序列例子验证等权工况聚合；
- 增加可视化网格分辨率不改变连续网络直接观测指标；
- 单/双卡、不同 batch_size 的指标一致；
- 绝对温度误差对 ℃/K 转换不变；相对误差有清晰分母定义；
- 缺失时段输出 N/A；重复环点不会扩大样本量。

### 多保真信息流（新增必须项）

- LF预训练原始数据必须只来自60个simulation train；两材料实际采样量均大于零；
- LF训练产物/POD/响应基/蒸馏教师不能包含simulation validation/test标签拟合；
- 校正为零时，HF输出等于LF输出（PRC与普通残差分别测试）；
- 冻结LF后HF校正阶段不更新LF参数，但LF坐标一阶/二阶导数仍正确；
- 联合阶段LF监督必须落在LF输出，验证LF分支确有梯度与参数更新；
- 两条数据流与DDP参数同步保持一致，不能绕过wrapper后只在单卡局部更新；
- 主路径不能由绝对表面G代数抵消LF；旧硬引导模式仅作明确对照；
- 推理仅给P与查询坐标即可运行，禁止读取该功率的任何实测温度；
- LF-only与HF-only仅是诊断消融，完整交付必须记录真实LF/HF共同来源。

### PRC 与物理

- forward 接受 `[N,5]`，返回 `[N,1]`；batch_size=1 也正确；
- t=0 和 P=0 输出符合已声明初始平衡条件；
- tau 正、有序，时间域内及测试性长时间查询数值有限；
- 正负幅值允许，但越界温度会在物理诊断中被发现而非导出时遮盖；
- 解析时间导数与自动微分一致，空间导数可二阶求导；
- 冻结 LF 参数后仍保留坐标导数；
- SiC/Cu 同坐标界面两侧不能按坐标去重合并；
- r=R 内侧极限连续，不只测试边界节点被覆写的值；
- 保存/重载预测一致；不存在检查点类型不识别时静默回退；
- 原模型启用硬绝对表面引导时，验证顶面确实不受内部修正参数改变；新可训练表面模型应有正确梯度。

数值容差在 float64 单元测试与 float32 工程测试中分别规定。不要让测试只检查张量 shape 而不检查物理/协议含义。

---

## 11. 运行与资源安排

沿用仓库已经记录的 Docker/PyTorch 环境，不为“追最新”主动升级依赖。先核对本地镜像和包版本，保存环境快照；不要求用户安装新的大型算子框架。

已有、可复用的入口：

```bash
python scripts/00_audit_data.py
python scripts/01_build_processed_data.py
python -m pytest -q tests
```

完整多保真训练调用之前，先核对 `--help`、当前 `identify/freeze` 参数与物理配置状态；默认配置含未解析项时不得跳过异常。最终命令写入运行目录的 `command.txt`，不依靠 README 中旧部署命令猜测。

资源顺序：

1. CPU 小数据单元测试，检查协议、接口和解析导数。
2. 单 GPU 做新模型冒烟与数值梯度检查。
3. 固定 seed=0 做小范围验证筛选，最多保留两个 PRC 配置。
4. 通过后运行 seeds 0–4；现有双 A40 环境可沿用 DDP，但每个 seed 必须有独立输出目录。
5. 最后才运行冻结的测试批次。

相同 epoch 不一定代表相同计算量。对照同时记录优化步数、观测采样量、物理残差采样量和训练时间。大模型不得因为额外调参次数多而被宣称为纯结构优势。

---

## 12. 最终交付与完成标准

### 12.1 文件交付

```text
reports/current_protocol/
├── README.md
├── data_inventory.json
├── protocol_audit.json
├── resolved_physics.yaml
├── metric_definition.md
├── low_fidelity_validation.csv
├── multifidelity_data_consumption.json
├── baseline_validation.csv
├── prc_ablation_validation.csv
├── fidelity_ablation_validation.csv
├── low_fidelity_final_test.csv
├── final_test_per_power.csv
├── final_test_summary.json
├── model_card.md
└── figures/

reports/releases/<release_id>/
├── release_manifest.yaml
├── config_snapshot/
├── environment.txt
├── command.txt
└── checkpoint_index.json
```

不必把大数据和所有训练权重提交 Git；但应有小型、去敏后的清单和关键结果随代码保存。当前全局忽略 JSON/CSV 的规则需要对最终小型报告做定向例外，避免公开仓库只有文字结论无法追溯。[R4]

### 12.2 必须能回答的科研问题

1. 当前固定测试协议下，现有强基线到底达到什么精度？
2. 表面误差来自引导器、空间回插、轴对称近似还是训练不足？
3. 使用了哪些原始仿真训练记录；相比去掉仿真的诊断模型，完整多保真是否更好？
4. PRC 的时间尺度校正是否比仅幅值校正、普通温度残差更有用？
5. IR 提升时 Hot/Cold 是否同步改善或至少没有明显退化？
6. 提升能否跨多个种子保持，代价是否合理？
7. 哪些结论由真实实验支持，哪些仅由低保真模拟或物理残差支持？

### 12.3 交付不是“必须 PRC 获胜”

若 PRC 验证失败，也应形成合格交付：完善的固定协议、可复现基线、明确的失败诊断、可部署的现有最优模型。

不能为了满足创新预期删除负结果、改测试划分或把仿真结果当作真实内部精度。

---

## 13. 当前还缺什么，哪些不应再次向用户索要

| 项目 | 现状/缺口 | 处理 |
|---|---|---|
| 数据的本地可访问性 | Git 未提交数据 | Codex 在用户主机检查/挂载 |
| 新协议训练证据 | 仓库主要数值仍是历史结果 | 当前固定协议重训、保留可核查产物 |
| 初始温度、光斑、吸收率、水冷面、环温单位 | 已有确认/配置 | 直接复用，不重复索要 |
| 热阻、辐射率 | 有有效初值/名义情景，未必可唯一辨识 | 解析状态并冻结；必要时训练内敏感性分析 |
| 真实重复实验/run_id | 当前提交不能独立核验 | 尽可能补采集来源说明；不虚增样本数 |
| 严格从未查看的盲测 | 仓库披露现测试曾被查看 | 诚实报告；未来有新实验再做前瞻评价 |
| 内部三维真实温度 | 当前不存在完整实验真值 | 不作完整体积实验验证声明 |
| 烧蚀几何是否显著变化 | 固定域模型适用性条件 | 据实验记录声明；若变化明显需另建移动边界物理模型 |

还有一项应从现有仿真设置核对，而非猜测：Gaussian 输入 P 是整束功率还是 SiC 面内功率。标准 Gaussian 在有限半径 R 内积分为 `ηP(1-exp(-2R²/w²))`；与实际遮挡/铜面受光及代码的功率约定须一致。不能一边在仿真中使用总束功率，另一边在 PINN 中无说明地归一化为全部落在 SiC 内的功率。

---


## 14. 可直接复制给 Codex 的执行指令

> 在现有 `temperature-field-prediction` 上实施本V2计划，保留 `configs/splits.yaml` 的所有划分，不恢复LOPO/LOGO。
>
> 本任务是低保真完整仿真场 + 高保真Top/Hot/Cold联合预测。仿真必须实际参与LF模型训练并在最终模型中提供基础场/响应参数，不允许只拿仿真做评价，不允许把纯实验表面拟合当最终方案。
>
> 第一批完成P0/P1：核对本地原始数据/缓存/检查点、数据和物理来源、直接坐标评价、等权指标、物理导数与DDP测试。测试温度不得进入任何训练标签、教师、归一化或调参流程；不查看测试误差。
>
> 第二批明确实现60个simulation训练功率的两材料全域LF预训练，用10个simulation验证功率选LF检查点。已有LF检查点仅在训练来源与当前协议完全一致时复用。冻结LF，用12个HF训练功率的Top/Hot/Cold校正同一个场；3个HF验证功率选校正模型。
>
> 联合微调时保留两条独立数据流：LF预测对仿真标签，HF最终预测对真实观测，物理残差作用在最终HF场。将当前HF输出向仿真回拉的损失保留为显式消融，不默认以其代替LF分支监督。冻结权重不许破坏坐标导数，解冻须验证真实梯度。
>
> 第三批再开发PRC：先从simulation train学习SiC/Cu全域LF幅值和时间尺度，再由HF训练数据校正幅值/时间尺度。比较V/A/AT；不能改成只用Top训练一个无LF来源的绝对温度引导器。纯表面拟合最多作为诊断，不是主线或完成标准。验证不支持提升则保留现有多保真基线，不强求PRC获胜。
>
> 全部配置、种子与检查点冻结后才执行LF/HF两套测试：LF输出验证仿真全场，HF输出验证169/339/634 W的Top/Hot/Cold。不要把HF与仿真的体积差值标成真实内部误差。所有结果记录LF/HF来源、采样量和真实运行状态。
>
> 未运行的实验不得伪造结果；不覆盖原始数据，不修改无关容器，不新建平行工程。每阶段更新进度并列出失败项和可复现命令。

---

## 15. 审查来源与方法依据

下列仓库链接固定到本次审查提交。文件后续更新时，Codex 应记录差异，以新代码的实际行为为准。

### 仓库证据

- **[R1] README / 工程能力与历史结果**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/README.md`
- **[R2] 固定数据协议与 splits**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/DATA_USAGE_PLAN.md`
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/configs/splits.yaml`
- **[R3] 进度与历史结果边界**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/PROJECT_PROGRESS.md`
- **[R4] 原始数据/产物忽略规则**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/.gitignore`
- **[R5] 边界与训练配置**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/configs/boundary_conditions.yaml`
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/configs/training.yaml`
- **[R6] 多保真训练器**（本次重点读取 1–190、330–830 行）
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/src/sic_cu/train/multifidelity.py`
- **[R7] 多保真模型与硬表面/边界行为**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/src/sic_cu/models/multifidelity.py`
- **[R8] 表面插值器、末帧保持与 Chebyshev 引导**（重点读取 1–230 行）
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/src/sic_cu/models/residual_interpolation.py`
- **[R9] 当前数据审计报告**（报告证据，不是本次重新实测）
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/reports/data_audit.md`
- **[R10] IR 评价与回插路径**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/src/sic_cu/eval/high_fidelity.py`
- **[R11] 当前负 z 几何约定**
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/configs/geometry.yaml`
- **[R12] 物理损失实现**（重点读取 1–200 行）
  `https://github.com/fei4440273/temperature-field-prediction/blob/adcf37f1082a53e5bc1522e6c0eabdc8d05b3969/src/sic_cu/losses/physics.py`

### 外部原始依据（只支持相关机制，不证明本项目预期精度）

- **[E1] Cao, Q., Goswami, S., Karniadakis, G. E. (2024). Laplace neural operator for solving differential equations. Nature Machine Intelligence.** DOI: `10.1038/s42256-024-00844-4`。支持极点—留数动态表示的背景，不支持本计划 PRC 的“首次”或精度保证。
  `https://www.nature.com/articles/s42256-024-00844-4`
- **[E2] PyTorch 官方 torch.utils.data / DistributedSampler 文档。** 支持补索引、分布式采样与评估去重要求；实际运行以项目安装版本为准，不要求升级到文档最新版本。
  `https://docs.pytorch.org/docs/stable/data.html`
- **[E3] Scikit-learn 官方 Common pitfalls and recommended practices / Data leakage。** 支持预处理拟合只使用训练数据、其他集合只应用既定变换的原则；本项目不因此引入交叉验证。
  `https://scikit-learn.org/stable/common_pitfalls.html`

---

**最终决策原则：低保真仿真学习完整场，高保真观测校正同一个场，物理方程约束最终场；固定数据协议贯穿全流程。先证明低高保真信息真实有效地被共同使用，再检验PRC是否提升精度，不以某个新模型必须胜出为完成条件。**
