# 失败与回退记录

- 系统Python的pytest因自动加载ROS插件缺失`lark`而未进入测试；使用项目原有PINN环境及禁用外部插件自动加载后，项目测试可运行。未修改依赖。
- 旧B0严格来源校验报告物理字节哈希和历史源码标识与当前不同。核对后仅为边界诊断字段名差异，解析物理配置、几何/材料和V4锁定哈希均匹配；新增受控桥接，不关闭新训练/发布的严格门禁。
- 单轮短跑验证选择分数升高，不作为候选配置，也不据此推断长期排程失效；状态保留，不覆盖旧B0。
- 未发生清理旧历史原件或冻结发布文件的操作。
