# 任-01启动验收：阶段状态与受控B0续训

- 登记时间：2026-09-15T17:18:24+08:00；不是历史训练时间。
- 历史起点：V4清单锁定的seed0 HF/LF检查点；仅允许清单哈希、固定数据协议与相同的解析物理设置。旧AdamW状态不存在，两臂均需以相同设置重建。
- 代码改动：新增包含优化器、随机源、冻结参数和采样器轮次的阶段状态保存/重载；独立物理梯度加入观测梯度；同索引拆包；可选B0起点和排程接口。旧训练默认排程和旧B0字节保持不变。
- 短跑预算：seed0、校正1轮、联合0轮、AdamW学习率1e-4、权重衰减1e-6、观测batch 2048、物理整包每项256点、传感器权重5/1、每轮验证。使用完整12个HF训练功率，不读取旧测试温度。
- 短跑验收：初始、观测最佳、物理最佳、校正末、训练末状态文件可重载；冻结LF参数不变但坐标导数保留；原始日志逐项注明优化器步数及物理消费。此短跑不评价模型收益，禁止据此采用排程。
- 后续预登记：任-02两臂都从同一seed0 B0开始，各续训300轮；学习率1e-4、相同batch/配点、每10轮验证；S0每轮15个数据步加1个完整物理步，S1每5个数据步并入一个加权子包，末尾不足5步仍处理。不改变数据划分和HF监督定义。任-02采用规则与总台账第六、七节一致。
- 受限项：原B0物理配置字节哈希因历史诊断字段名变更与当前不一致，已核对其几何/材料字节及解析物理语义；属于带历史来源的受控续训，不宣称历史源码逐位重现。多卡随机状态保存尚未验收，先只运行单卡。

## 保存—续跑附加验收（2026-09-15T17:33:34+08:00登记）

两臂都从同一锁定B0开始，预算各为2轮、校正/联合=2/0、其余与上面的短跑完全一致。第一臂指定单次会话最多1轮，暂停后从本运行`阶段_最近.pt`续跑第二轮；第二臂不中断跑两轮。提前固定验收：两臂日志各两行，LF仍冻结，每轮消费15个观测数据步及256个物理点；末态模型参数逐项一致或处于float32容差内，验证分数差不超过1e-4 ℃。恢复不读旧测试标签，不改变任何训练预算。时间以真实命令开始/结束为准，本节登记时间仅供预注册，不冒充执行时刻。
