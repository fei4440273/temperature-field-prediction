# 任13原RZ四臂20模型同网格真CUDA推理分项受限验收

现场日期：2026-09-16（北京时间）。结论：录-0068已于05:55:44+08:00事前在主账同一行登记固定预算YAML SHA256=`b82b2c540ce12eeb4d8b8f8677c466211e1860fd301299c224770b8236602d42`和十二源tar SHA256=`75725882f8f865a388e0da6cd0d423c5b6c8c1b1db68bb46bf7759190a0752b4`，真实任务13 CUDA运行于06:02以后退出0并实际生成四臂各五种子20/20份同网格结果。**只验收RZ原装置在本次硬件和309 W已知HF训练功率处的分项推理耗时；不验收模型预测改进、总离线成本、名义工程能源或独立新实测。** 旧B0部署不变，新发布配置仍为草稿。

## 固定合同与来源

使用原10 W处理parquet只解码`time_s,r_m,z_m,material_id,node_label`五个结构列，101帧、每帧1159个原RZ节点（Cu821、SiC338），查询0—200 s每2 s一帧、功率309 W为原HF12训练折内已有功率。没有读取对应功率的HF实验温度，也没有读取旧固定TEST 169/339/634 W温度。B0、历史MLP、正式E0、正式F2各五个原HF最佳权重和真实训练收据，加上两种LF方法各五个原权重与预训收据，共60来源文件已先由预算登记SHA与项目现场逐件核对；正式入口再次核主账双SHA、12源成员原字节及20 HF/10 LF身份，CPU定向合同测试30/30。任务10人为一维板或FEM不参加RZ模型成本排名。

本次采用单卡NVIDIA GeForce RTX 4090 D、驱动550.163.01，PINN环境torch2.5.1/CUDA12.1、numpy2.0.2、polars1.36.1。每份同batch2048，预热2次、设备同步后的纯高保真模型前向重复5次、输出回读一次；各份同格式导出`field_rzt.npz`、`metadata.json`、102行冷热CSV、0/50/100/150/200 s五份theta=12旋转vtk及4张图。若有生产硬件或部署batch变化，不能搬用本次秒数。

## 真实分项数字

以下每种方法先取每seed的5次同步纯前向中位数，再对五seed取均值和种子间样本标准差（ddof=1）。旁列同格式过程场导出是五seed平均秒，**不能**把五次重复前向与仅一次网格读/导出或离线训练成本相加当作全寿命成本。全部原五次秒数、读网格、重载checkpoint、查询构造与设备传输、输出回读及可复用LF成本各自保留在项目原JSON中。

| 模型臂 | 纯GPU前向五seed中位均值 ± 样本std，毫秒 | 同格式导出均值，秒 | 网格五列读取均值，毫秒 | 模型checkpoint读取与GPU重载均值，毫秒 |
| --- | ---: | ---: | ---: | ---: |
| 历史B0 | 28.668 ± 0.882 | 0.489309 | 5.420 | 14.649 |
| 历史MLP | 18.715 ± 0.218 | 0.465140 | 3.644 | 2.696 |
| 新E0乙 | 28.304 ± 0.217 | 0.453895 | 3.992 | 4.700 |
| 新F2受限 | 28.066 ± 0.122 | 0.462190 | 3.819 | 5.701 |

本次相同RZ场、固定查询批次下历史MLP纯前向观测更快；这是实际耗时事实，顺序按B0、MLP、E0、F2逐臂运行，不是打乱顺序/重复多日的硬件因果性能证明。MLP旧协议的观测稳定性反例、LF架构不同与旧训练源码补丁缺口，另见任务11历史受限报告；不能仅凭毫秒采用它或推断其工程物理合格。导出过程约0.45—0.49秒/种子，确实不同于只含网络前向的18—29毫秒。历史B0/MLP训练收据各对应单次完整1700轮会话，E0/F2收据是最近**断点训练单会话**，两类并非完整训练可比总墙钟；真实原LF FEM仿真生成墙钟未得，LF网络预训可以复用但也未构成闭合总成本。

## 原件、复核与不能推断项

正式目录：[四臂原结果](正式原RZ四臂20模型CUDA分项计时_20260916T060226+0800)，其中唯一汇总`四臂五种子合法推理分项与成本.json`实际SHA256=`b902f59edf6102c3d6adea8f2f9d36f2a786bc0de0c27ad35aaee990860d2020`。ROOT独立比对20份原模型SHA与事前注册20身份、20份各12个导出文件共240个现场SHA与逐模型`输出SHA256`、20个原RZ完整场`mean_temperature_c`均为有限`101×1159`、每份坐标`1159×2`、冷热CSV102行、5次有效同步真计时，全部PASS；逐份含`旧TEST温度读取=false`、`同功率HF实验温度读取=false`。每份`计时分项与来源.json`与总JSON同源索引原字节一致；程序EXIT0且运行后GPU无该任务在途进程。

十二源tar锁的是关键源集合，**不是完整执行依赖**：执行中的模型构造`src/sic_cu/train/simulation.py`未入tar，运行前/后的现场SHA256均为`206dca232f43e0cbf94ae1547368346e4898815c5b418531837f27be37c7835a`；坐标缩放`src/sic_cu/models/common.py`未入tar，两次SHA256均为`4e99edc4d470330d42d4bd92e36c4d7ad73f10ba71195606e0ef1e1e2d171a41`。环境依赖和历史dirty训练代码也没有得到全字节逐位封存，故只报告本次实际现场审计，不承诺在未知环境逐位重现。

任08真F2虽合法观测五seed宏`1.997623±0.089677℃`，独立观测最佳原64阶绝对能源平衡宏约`2205.722 W`且物理第0轮更差；恒等式数值自洽不等于原FEM工程预算合格。缺少原装置HF内部真实实验温度与独立新增测温，推理场数值不是新盲测真值。任务09三档完整效率曲线、任务10真实25模型全组隐藏折、任务11新同合同重训和任务12独立实测未完之前，本报告不构成任务13发布或总计划100%验收。
