# 任-02失败与回退记录

- `任务02_S1单轮门禁_种子0_20260915T174355+0800`：2026-09-15T17:43:55+08:00启动，训练更新后在独立16阶能量审核的float64坐标与实时float32 DeepONet权重相乘处报`RuntimeError: mat1 and mat2 must have the same dtype`。退出码1，`training.jsonl`为空；初始及阶段文件是未通过审核的部分产物，不能据其宣称排程成功或有预测收益。
- 根因：既有V5只读审计先冻结并将加载后的模型副本转成float64；新监测误将float64求积点直接喂给float32训练模型。未涉及数据划分、B0或优化器回退。
- 有针对性的回归：新增真实float32 DeepONet四点审核失败测试，改为复制冻结模型副本并在副本中转float64，实时模型/优化器仍为float32；定向测试通过后用新的目录重跑门禁。原失败目录和空日志不覆盖、不填造后期指标。
- 若重跑仍遇来源、梯度、能量或日志异常，则继续定位；正式300轮不得从本次失败状态接起。

- `任务02_S1单轮复验_种子0_20260915T174710+0800`在新目录核对真实15个优化器步、3个物理子包、每项256点和4条独立能量分项；该1轮只解除实现门禁，不并入正式300轮统计。
- 正式seed0 S0/S1各300轮后，S1相对同预算S0的验证选分仅下降1.285%、完整绝对能量均值下降4.347%、95分位恶化0.434%，未过物理修复门槛；S1不采用，保持原S0排程。历史B0检查点仍优于这两臂的观测分数，保留两份失败/无收益产物供检索。
- 原训练`metrics.json`顶部30/100秒时间窗端点错位，记录原件，不重写旧运行哈希；两臂最佳状态的合法验证观测同步重算中文窗口表，选择分数不变。后续候选统一使用正确`[0,30]`、`(30,100]`、`(100,200]`口径。
