# 任-02：同一B0下冻结LF排程对照

- 预登记时间：2026-09-15T17:40:22+08:00；正式训练尚未执行。
- 唯一起点：V4清单seed0 HF最佳轮次579与配对LF检查点，HF SHA`0764c7e78f45e3509f0f8a525965a8f9d77a789aed47ce10ea8d536673644f4b`，LF SHA`5ab23ffd5b7c4b99ba90b81236ec0fba4a9b109a730288124c647030036e42f2`。两臂均无旧优化器可恢复，重建相同AdamW。
- 单卡短跑：先仅对S1跑1轮，确认15个观测更新并入3个物理子包，共消费每项256个配点，体内子包均含SiC/Cu，界面双侧成对；1轮不用于配置选择。
- 正式预算：S0/S1各续训300轮、batch2048、学习率1e-4、权重衰减1e-6、物理整包每项256、传感器绝对/温升权重5/1、相同随机种子0及训练数据顺序、每10轮检查，单卡，联合0轮。两臂LF一直冻结，原时间特征、教师、边界、物理权重、仿真标签均不改。
- S0每轮15个独立数据步后做1个整包物理优化步；S1每五个数据步在同一步追加整包的一块加权物理梯度，3块的点数与权重合计严格等于整包，末尾不足五步也处理。S0/S1优化器步数可不同，不称Adam轨迹完全相同；两臂不插假零梯度步。
- 每10轮共同输出HF验证分数、顶部/环温、LF冻结哈希、独立固定配点的PDE/边界/界面残差，固定功率`[55,630.5] W`与时刻`[10,100] s`的16阶能量分项。该四点低阶监测不是V5完整能量资格。
- 末态及候选最佳采用V5确定性64阶、6功率`[55,115.2,364.3,403,630.5,729] W`×5时刻`[1,10,50,100,200] s`的30行独立审核；分项及相对分母原样保存，报告绝对平衡均值、95分位和逐行值。局部残差使用固定审核点，不与训练配点混用。发现新候选16/64阶不一致时不得按64结果作合格断言。
- 同预算采用条件沿用总计划第六节：优先HF精度不退化超过2%、完整30点绝对能量宏均值改善至少50%、95分位不恶化且局部残差不显著恶化；没有收益保留S0并记录候选失败。名义5%/10%吸收功率归一筛查与实际内部真值资格分开，原有限元参考阈值缺失时相关项目写“未定义”。不足五种子的先导不作为正式稳定性结论。
- 禁令：不读取旧固定测试温度，不改变划分/网络/损失尺度/物理参数，不覆盖任-01短跑或B0；单轮门禁不能混入正式300轮。任-02无论是否有收益均继续任-03的LF精度修复。
