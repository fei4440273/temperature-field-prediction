# 任-04：正式冻结臂GPU入口权限诊断

登记时间：2026-09-15T21:05:18+08:00。本记录只说明入口失败和权限环境，不构成模型预测精度或能量修复结论。

先按预登记命令`PYTHONPATH=src /home/phl/anaconda3/envs/PINN/bin/python scripts/19_run_task04_joint.py --arm 冻结 --output 研究记录/任务04_联合微调/任04_冻结LF_种子0_20260915T210001+0800 --device cuda`在默认沙箱启动两次，均在`fork_task04_model → AdditiveCorrectionModel.to(cuda) → torch._C._cuda_init`报`RuntimeError: No CUDA GPUs are available`。实际源校正末检查点SHA及预登记已核对；失败时尚未创建该输出目录或写训练日志，**不计正式轮次，不生成候选**。

只读探针显示：直接运行相同PINN Python创建`torch.empty(1, device="cuda")`成功；带相同`PYTHONPATH=src`前缀的纯PyTorch探针在导入任意项目模块之前失去GPU可见性。独立诊断排除本次起点模型、LF来源以及训练模块修改设备环境变量的路径；用明确获准的宿主机GPU权限运行相同探针及真实起点模型加载均成功。因而不改变已归档训练源码、源检查点、配置或实验排程，只对原正式命令使用已审批`sandbox_permissions=require_escalated`。这一权限差异属于**入口环境故障**，并非通过改超参数取得的新精度收益。

获准的同命令随后创建原输出目录并写入逐轮日志，至本次登记已超过80轮、每轮15次HF观测优化、1次256点独立物理优化和60个LF训练功率各2048点的真实回放；现在仍在运行。结束前不得记200轮验收，不得进行针对最佳／末态的完整30点能量比较；结束后仍需独立核真实消耗、可重载检查点及配置来源。

真实同源起点是[任-03旧LF HF完整校正末](../任务03_低保真精度修复/任务03_HF旧LF同源_种子0_20260915T184956+0800/阶段_校正末.pt)SHA`1425114fcded5221f8b3a4c4bafda56a5ac2e12ea2bcb14e1bd40c6c6272f81e`；[任-04事前配置](有效运行配置.yaml)SHA`b464eddd752000694a513da0e4a1b4b0f7447e31b85d19726e243396a0864710`；[修复后源码档](源码快照_续跑事务修复后正式启动_20260915T205804+0800.tar.gz)SHA`719fbc97a58a9c11f7911585038d0fc0ce4a90a88de7c6c8ef7952157a47e756`。后续有限解冻臂仍必须从同一登记源独立分叉，不能从冻结臂最佳或末态接续。
