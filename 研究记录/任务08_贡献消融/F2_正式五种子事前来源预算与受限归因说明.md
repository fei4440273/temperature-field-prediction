# 任08 F2正式五种子：事前来源、预算与受限归因说明

登记时间：2026-09-16 03:46（北京时间）。**此处仅完成正式运行前封存与CPU门禁；真CUDA正式五seed训练、合法HF五seed结果、六状态独立工程能源均尚未生成，不得据此宣称F2优于F3或原工程物理合格。** 任07已冻结为乙，历史B0部署不变。

## 唯一受检训练差异

F2是任07 E0无新增特征多保真DeepONet的专属另版入口，使用V4 B0原五seed配对LF检查点的各自全部参数和缓冲区；同seed旧HF检查点仅借旧六列架构，新HF权重以冻结E0函数分别重新初始化、AdamW空状态与Python/NumPy/Torch CPU/CUDA四随机源同源起步。HF仍为12训练功率/3合法验证功率，LF为60真实仿真回放训练功率/10合法验证功率；每轮HF顶部IR29593点，按2048分15批，Hot/Cold真实训练环温2985点在每批共同消费，总44775次传感训练点消费，另1次256配点物理更新。校正最多1500轮，受限联合最多500轮；每10轮合法HF宏选分/分模态、Cu/SiC两材料节点及真体积四口径LF审核，改善阈值0.0001、早停耐心200轮。联合只解冻四个LF末投影并在各轮回放60批乘2048个真实LF点，原LF其余参数/缓冲区继续冻结；冻结LF参与HF前向与空间坐标导数，不能以`no_grad`切断。

观测训练HF IR/HotCold/LF回放权重与F3均为1/5/1/1；单一实验因素是**HF训练物理配点明确`compute_pde=False`、PDE权重0、`pde=None`（未计算，不代表零误差）**。初值、轴线/顶部/暴露和水冷边界、SiC-Cu界面等训练软约束均权重1且继续反传；LF先验本身曾接受物理约束，共同接触参数依然源于LF辨识，故不能称完全无物理或HF-only。共享`configs/training.yaml`及冻结任07正式入口、旧任07物理源码归档与模型均不改。独立模型诊断使用完整`compute_pde=True`、PDE权重1（含PDE及三项其余物理），30功率时刻点×16/64阶真实瓦数能源只审核、不参加反传；训练非PDE损失不得直接和F3含PDE训练总损失排名。

## 真输入和历史限界

事前登记逐字SHA锁真实处理顶部IR `dd8ef7e112bce2bd73dd3fe3ad5889131964db4af1b57c882b43f5b816d4b82d`、Hot/Cold环温 `c987bafd68a37d1ab30857a834783764bde1c10249e8c02147118c02b3162db4`、处理manifest `1ea58fded68b8950d89c9012d879a7b6197dbb2b12c3c3ccc44af474843d8a82`；LF训练60与验证10各真实`data/processed/simulation/<功率>W.parquet`全部逐文件SHA列在专属YAML，启动和续跑均重算拒绝字节漂移，不读旧`test_Data`温度。任07既有事前来源登记原件SHA `e5c4795952abb02662fe3172d5a32b5a8024926a32bd63db01447051d14e3c2d`确证原顶部IR与处理manifest两份SHA等于当前；来源协议、各seed LF检查点与初始张量独立与冻结任07对齐。

**因果限界：**任07 F3原事前档没有单独封存当时Hot/Cold处理parquet与LF70训练/验证parquet逐文件字节SHA，因此这两类数据仅有同已锁处理manifest/路径/数据折及本次逐字锁定，无法事后从不存在的历史SHA证明它们与旧F3 CUDA训练时逐字相同。即使F2与F3后续合法HF分数有差别，也只能注明这一来源限制、实际早停轮次差异和原任07乙物理不合格；**不能声称已证成严格逐字单一PDE因果贡献、原装置内部HF真值或能源闭合**。如需严格字节单因素重复实验须另行事前冻结共同HF Hot/Cold和LF70并配对重训F3/F2，不能涂改任07已冻结历史。

## 事前源码与入场核验

项目内全新源码tar `F2_正式五种子事前源码快照_20260916T031552+0800.tar.gz`实际SHA256 `6bdbc036a0c2bb01e95fc2cb56afc0d30a54885c6e8302b1b02676ffb4d4838d`，23个成员逐文件SHA与当前运行源码字节完全相等，且运行入口逐项核验成员、原件与tar SHA。新独立训练器`src/sic_cu/train/task08_f2_formal.py` SHA256 `f798ef01e19cf016753ac4313ce89ce5b716f1aa4d9f85bc41847f6ccf28efce`；CLI脚本32 SHA256 `5ef736d582faa60d72452181bf389fec2a0dd68af2490fbb25aa683065abbd52`；独立五seed状态/能源审核脚本33 SHA256 `fb3c3f6a3da776eeb7f2f73a66a2ed3468cdac0a0bc3d017b4619f9e24ecc816`。专属事前有效YAML `F2_正式五种子有效运行配置.yaml` SHA256 `4cc28923d22af6e2118f7dce34d042f4675c275fbf76a1f972cdc89cede106af`，以结构化配置重载与独立重算五源、当前70 LF输入、HF三输入、23源码成员严格相等。

CPU门禁仅用于来源和实现：seed0真实HF两轮校正、一轮60批真实LF联合、再一轮真实LF联合从最近完整模型/AdamW/四RNG续跑，每轮15＋1步、256物理配点，训练`pde=None`、其它物理项保留，四末投影训练且原LF其它张量逐位不变；旧历史best、校正切换SHA、模型视图与最近阶段均有拒绝伪装门禁。项目内fresh F2独立入口/独立审核/CPU来源测试及任07冻结源、共享物理默认路径、合法径向审计相邻回归均退出0；所有pytest临时文件仅位于项目内`研究记录/临时任08F2_正式入口_20260916T024550+0800`。CPU短诊断不计正式训练轮次，不形成五seed成绩。旧入场报告记载的偶然项目外`/tmp`测试临时工件仍原样保留、不作为本次验收证据。

正式CUDA必须先由总台账第九节唯一`录-0047`逐字登记上述YAML和tar的实际SHA并完成独立门禁；随后先seed0真CUDA10轮检查来源/完整RNG/预算及同seed续跑，再依既定早停边界完整训练五份，分开审计每seed第0轮、观测最佳、物理最佳和真末状态、五份合法HF逐模态/分窗/径向及十份观测最佳/真末独立工程能源。**本报告写入时上述任何正式运行与性能、能源结果仍为零份。**
