# 任-09三条嵌套HF功率序列只读来源与正式入口限界

核验时间：2026-09-16。此文件只是此前已封存功率序列及处理数据的**不读温度元数据**核查，不运行3/6/9/12功率模型训练、GPU或跨方法排名。原额外功率覆盖事前登记SHA256`206b91e3ea981ec81c0ed1074ea4688a16567e32cab9098b8c134ae683becd6a`和`configs/splits.yaml` SHA256`89a1ebd7df4512ea1ffc1704799cc120182139e12d08e35d1dcb800ae9cc296e`已各按原件实算相符；任-09正式数据效率曲线尚未开始。

## 功率及观测来源

按`研究记录/任务09_高保真数据效率/额外功率覆盖序列前登记.yaml`的结构和现行功率规范化键核实：总计划主序列以及额外左中段约309W、右中段约430W两条，各自都满足训练功率`3⊂6⊂9⊂12`，所有序列固定55W与729W端点；任一子集均没有混入HF合法验证115.2/403/630.5W或固定测试功率，12功率终点相同。总计划主3/6/9功率逐项与其原第九节之前的主条款一致。LF训练60个/合法验证10个功率保持原协议。

只扫描了处理数据的`split`、规范功率键、观测类型元数据列，不读取温度列或旧测试文件：HF训练12功率**每个**均有顶部IR、Hot、Cold，HF合法验证3功率**每个**亦有三模态。现有`src/sic_cu/data/splits.py`的`resolve_hf_training_subset`拒绝非法功率，`src/sic_cu/train/multifidelity.py`数据构造可按**同一HF子集**同步筛IR及Hot/Cold、保持固定HF合法验证和LF60来源；处理数据与历史检查点协议互验参照`src/sic_cu/eval/protocol_checks.py`。这确认了数据入场可行性，**不能代替正式训练**。

| 已登记HF训练功率数 | 主序列真实训练IR行数／批次，batch2048 | 左序列批次 | 右序列批次 | 数据限定 |
| ---: | ---: | ---: | ---: | --- |
| 3 | 6868／4 | 4 | 3 | 三模态必须随同一子集同步缩减 |
| 6 | 15150／8 | 7 | 7 | HF验证3功率始终不缩减 |
| 9 | 22624／12 | 11 | 11 | LF仿真训练60功率始终不缩减 |
| 12 | 正式任-07原HF训练集合约29593／15 | 15 | 15 | 同一12功率终点只允许合规冻结后复用，不必重复训练 |

上表批次数来自实际训练IR行数的向上取整，左/右3/6/9暂只核批次数而不把未查IR总行数冒充已核值。子集不同导致真实观测样本及批数不同，**不能用“每轮同15批”掩盖不同真实预算**。现行任-07 E0正式入口强制完整HF12训练集合、每轮15个HF批2048点/一次256名义物理更新、受限联合LF60×2048分4组并入15个HF步，不提供3/6/9子集参数；强行把旧通用训练脚本叫任-07 F3亦不合格，因为旧联合会解冻全部LF而非四末投影。

## 冻结与可比性条件

任-07五种子E0须先完成真实训练、合法观测/名义能源双轨与冻结关卡，才可复用12功率F3正式值；本轮F4时间增强已不采用。3/6/9每个F3缩减臂均需用与该seed原LF配对的**新HF随机初态和空AdamW**独立训练，不能借12功率HF最佳/中间权重，更不能把通用旧全LF解冻当作四末投影。F1 HF-only须独立从零创建DeepONet-PINN，不能用`task07_source.py`的LF初始化、LF字典或LF标签；该方法现在没有真实HF-only检查点。如F1使用原经LF训练辨识的接触热阻，必须单列为共享LF校准参数的额外消融，不称完全HF-only。未来各功率子集必须事前登记实际批数、监督/物理配点/优化步、合法选分/同早停、完整阶段及五seed实际耗费；不修改当前三条序列或事后删除不单调点。

现阶段结果是**预登记与数据只读门禁通过，正式误差/数据效率改善无结果**。任-07进入状态丙时，不启动任-09跨模型数据效率排名。
