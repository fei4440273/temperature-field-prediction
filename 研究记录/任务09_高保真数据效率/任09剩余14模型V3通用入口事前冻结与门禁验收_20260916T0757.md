# 任09剩余14模型 V3 通用入口事前冻结与门禁验收

## 一、结论

本次只完成任09主序列剩余14个正式模型的 V3 通用训练入口、能源包装入口、事前 YAML 和源码 tar 冻结。登记时新模型数为 0、GPU 训练轮次为 0、双能源审核数为 0，不构成 HF3/6/9 五种子数据效率曲线或精度改善结论。

已完成的 `primary/HF3/seed0` 仍依录-0075/0077管理，不进入本 V3 的14个身份。新入口只接受 `primary/HF3/seed1-4`、`primary/HF6/seed0-4`、`primary/HF9/seed0-4`。

## 二、事前冻结原件

- V3 登记：`正式F3主序列剩余14模型通用入口前登记_录0078.yaml`，SHA-256 为 `87816d50efe82fc7a5c218707b1323a1b0d4a8cfd9eceb7c7771bb63c9edfb28`。
- V3 源码归档：`任09F3主序列剩余14模型v3源码冻结_20260916T074940+0800.tar.gz`，SHA-256 为 `695e8b6c9ae9847ed2576609777dee5750c2f84c69f10667a69cd2a27a43765d`。
- tar 仅含14份普通文件，无目录、链接、绝对路径、`..` 或额外成员；逐成员 SHA-256 与现场原件一致。
- V3 trainer/CLI/登记器/能源 CLI/测试 SHA-256 分别为 `be6b6ccfe22e983f6147fa41c0f25ca9db872601558d50c78e6d85137e4e45f4`、`eb3ec260e1d03497ae2d93f481da7bc22f0ff49f115a9308726c4bd437d14a54`、`7398c9f2f34f2066667b40e1a231eca4db932b5921b38f42522d6077e4762762`、`76fe717f55e54856b8e494db7efb7e3eb7273aa006266e8a077b743608d92666`、`7528a1fb86556e76549bb61424653346efe6da9614d86642694d2483f560d158`。

## 三、门禁与运行约束

1. 14个身份各自绑定唯一的 `正式F3V3_..._录0078_...` 目录；既有目录不覆盖，失败孤儿不得直接复用。
2. 每次会话最多200轮，不足200轮只允许真实终止或精确剩余轮次；每段提交不可覆盖的会话收据链。
3. 活动日志必须与最近完整 checkpoint 精确一致；多余尾行和临时文件拒绝自动裁剪。
4. 校正早停恰逢会话边界时，只在 recent 与校正末态逐值一致后原子补交末态 SHA，不增加训练轮次。
5. 训练和能源入口均先核录-0078同行 YAML/tar 精确双 SHA、V3 manifest、会话收据链和三份末态。旧 V1/pilot 产物不能冒名进入 V3 下游。
6. 同一身份从预检到最终收据持有项目内普通 `.lock` 文件的跨进程非阻塞排他锁。锁文件是运行辅助原件，不是模型产物。
7. 只有 JOINT stage、校正/联合各至少200轮且在10轮边界真实截止，并绑定校正/联合/训练三末态 SHA 和真实截止报告后，才能进入 best/final 能源审核。

## 四、独立验证

- 冻结后项目内新鲜回归：Task09 subset gate/V1/V2/energy/V3 合计 `86/86` 通过，其中 V3 `24/24`。
- V3 trainer、三个 CLI 和测试的 `py_compile` 通过。
- V1 trainer、V2 trainer、V1 CLI、V2 CLI、旧能源模块和旧能源 CLI 六个冻结 SHA 全部未改变。
- 在录-0078未写入时，用真实 YAML/tar 双 SHA 调用 HF3/seed1 正式 CLI，入口报“ROOT总台账尚无录0078登记，禁止GPU”；目标模型目录未创建、GPU计算进程为0。仅留下设计内的0字节 HF3/seed1 身份锁原件。

## 五、不变项与限界

录-0059的数据折分、HF三模态、LF60、合法三功率验证、每轮真实消费和1500+500数学上限不变；录-0075的 AdamW step 语义保留。不读取旧固定 TEST 温度，不修改旧 B0 部署。任12仍因没有新的外部独立真实测温而受阻；不用合成数据或旧 TEST 温度补该缺口。

下一步只允许在录-0078精确机器标记生效后，单 GPU 从 `primary/HF3/seed1` 首个200轮会话开始。首模型须先完成会话链、真实联合终态和 best/final 双能源独审，再推进其余13个身份。
