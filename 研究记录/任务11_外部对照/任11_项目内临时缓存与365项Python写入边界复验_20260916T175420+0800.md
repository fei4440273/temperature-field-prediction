# 任11项目内临时缓存与Python写入边界中文复验

记录标识：20260916T175420+0800。本记录是执行边界补充，不重写旧184项封存功能证据，不增加实验预算。

## 真实结果

| 根代理核验 | 完整终态 | Python项目外写入尝试记录 |
|---|---|---|
| Howard LF及数学/旧模型/LF邻居184项 | 184 passed in 19.05s，退出0 | 空列表 |
| 新MLP观察、Howard HF初始化及上述四套共365项 | 365 passed in 48.06s，退出0 | 空列表 |

365项执行时HF初始化仍是124项版本；新增24个存储别名反例及观察入口独立复核所提问题的修复后验收另记，不能用本报告代替。

## 边界与更正

此前封存前184项18.70秒的功能回归只给了pytest的basetemp，没有显式指定依赖TMPDIR；继承环境中TMPDIR未设置，Torch依赖可能自行选临时目录。该功能结果可以保留，但不能据此断言所有临时写入项目内，也没有证据可将可能性冒写为已确认的项目外文件数量。

后续命令已显式设置项目内TMPDIR、CUDA_CACHE_PATH、XDG_CACHE_HOME、MPLCONFIGDIR、TORCH_EXTENSIONS_DIR、TORCHINDUCTOR_CACHE_DIR，并关闭字节码和pytest外部插件/缓存，CPU测试屏蔽CUDA。根代理安装Python路径审计hook，拒绝项目外可写open以及路径删改、目录创建和链接事件。

第一轮严格审计在测试开始前拒绝pytest logging插件写入/dev/null，退出3。这是核验配置与审计边界的冲突，不是训练代码失败；仅关闭logging插件后回归正常。成功两轮均无被阻止的项目外写入尝试。

本证据仅证明所覆盖的Python审计事件，没有全OS系统调用跟踪；未发出Python审计事件的C++、文件描述符写入或子进程写入不能据此宣称已完全审计。子进程继承项目内临时缓存环境，不自动继承根Python审计hook。没有对任何项目外文件作删改或补救清理。

## 限定

观察入口所声明的四份既有HF来源文件均存在，实际SHA与声明逐件相同；此探针只读注册/归档/身份清单原件，不读取正式PT或PARQ温度。测试均为CPU组件证据，不是正式观察、Howard HF训练许可、预测精度改善或工程安全验收。旧B0部署与旧TEST封印不变。

完整实际命令、审计程序、退出码与终态输出见同名JSON：任11_项目内临时缓存与365项Python写入边界复验_20260916T175420+0800.json。

