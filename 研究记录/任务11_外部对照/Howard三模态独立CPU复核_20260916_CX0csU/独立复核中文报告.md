# Howard 三模态组件独立 CPU 复核

## 结论与限定

本次对当前三份 Howard OBS 候选逐行独立审查，未发现新增已证实的 P1/P2 缺陷。本人重新执行作者当前 83 项，实际 exit 0，83 passed in 41.41s；另编独立 29 项并实际执行，最终 exit 0，29 passed in 39.06s。这是两次实跑合计 112 项通过，不是一次 112 项测试，也不代表正式观察或能源完成。

此次只使用本项目隔离目录中的合成 CSV、Parquet、普通源码副本、ROOT 副本和合成 PT。没有打开正式 PT、真实 Parquet、真实温度、LF TEST 或固定 TEST，没有初始化 CUDA，没有进行正式 OBS、能源审计或训练。没有修改三候选、父 HF82、旧 OBS72、能源三源、既有 YAML/tar/catalog、活动 ROOT 或三全局状态。所有新文件、临时和缓存均在本项目内，未安装、删除、回退或覆盖用户物料。

## 当前候选身份

以下为实跑前后及封存现场核验绑定的完整 SHA256；字节数以同目录机器证据现场 stat 为准。

| 候选 | SHA256 |
| --- | --- |
| src/sic_cu/eval/task11_howard_observations.py | 0db2f84797d27f5933e8af46dfc2df820ce0e18be512c7a6e5a9e9ad2b5ff807 |
| scripts/58_export_task11_howard_observations.py | 11ba79106d63fb2040b37ee6d3be18e83fd31eeeb81913718399df476f528368 |
| tests/test_task11_howard_observations.py | f6092c84f442939f3c849bf83054f395a873e00690643b5c61b52e34f680cae3 |

HF82 与旧数值 OBS72 的并集为既有父闭包87；加入本轮三源得到 OBS SOURCE90，普通包不含 Howard 能源三源。复核实际前后跟踪全部93 = OBS90 并能源三源，同时分别保存父87、HF82、旧72、能源三源、候选三源及固定父六件完整身份，每轮均逐件零漂移，封存再次现场核验。HF四件为事前登记、普通源码 tar、本人 LF 清单、HF 开发结构来源清单；另两件为既有独立能源登记与 tar，全都只读。

真实 ROOT 整件身份仅作各轮历史上下文，父执行者可合法追加台账，本次不把跨轮 wholeROOT 差异误记为冻结源码漂移。独立合成 ROOT 病例实证：无关合法追加不破坏活动六 SHA 解析，但在同一审计窗口内会触发 wholeROOT 初始 pin 漂移拒绝。六 SHA 只属于 OBS 自己的登记/tar 与父四件，不能借能源或 MLP 许可。

## 独立检查与真实 IO

已经完整读取三候选及按需父只读依赖，重点核查修复后的真实 writer，而非仅复核假 mock 数学。作者已有写后 source/model 漂移四项 RED 与最小修复证据由其档案绑定；这些作者历史 RED 不是本次独立实跑的新发现。本次独立 83 包含改后的四项并全绿。

独立 29 项覆盖如下边界。

1. 原生 Float32 径向端点及相邻 ULP，完整窗口 [0,8]、(8,17]、(17,25] mm；合法 VAL 三功率、72 原帧、101 半径/帧、25 mm 端点均保留。时间边界与首个真实观测时刻作差，不用人为零时刻。
2. 经既有 radial_observations 实际读取合成原始 CSV，产生截断 inverse-variance 原 reliability_weight_raw 和原 Float32 frame_weight；经真实 processed Parquet 与 canonical ring loader 取得7272 Top、752 rings。真实合成 Parquet 读取2次，混入的 train 行不进入 VAL 输出。逐点原帧权重原值严格保留，不对径向窗口逐帧再归一。
3. 十个实际 torch.save 合成 Howard best/final 视图经 torch.load(weights_only=True, map_location=cpu) 真实反序列化，再按自己 seed/固定 own-run 路径/字节 SHA、56个 Float32 参数与两个 Float64 固定查询 buffer 严格 factory。计数10合法视图与2个非法 dtype 视图共12次合成 torch.load；严格 factory 验证先于 CPU device move，非法 query32/parameter64 在 move 前拒绝。没有对 CUDA 执行顺序作实跑断言；正式导出源码的 model_from_view(...).to(device) 顺序已静态核查。
4. 真实计算十态80240逐点结果与90功率、270时间、90径向汇总行，功率等权宏指标、原权重误差、raw p95、有符号峰值及五 seed ddof=1 统计通过。独立合成数据可分辨错误的窗口逐帧重归一：原值 RMSE 3.319419633559334，而错误重归一对照为3.3317369783955963。
5. 独立构建普通 SOURCE90 tar 的合成副本，硬链接、目录、缺员实际拒绝；作者83另覆盖链接、重复、额外。十模型 catalog 的跨 seed、LF 路径、bool seed、重复 view 别名实际拒绝。全部为合成字节和路径，不是正式十态训练真实性验证。
6. 对 OBS 全来源收集器实际调用并检查107静态初始 pins、85附加 pins，重叠去重后182件。这里只将 HF 资格 API 接到完整合成 own-run 资格字典，LF/HF 原件、数据来源也为合成副本；该夹具不授予正式五 HF 完训资格。公开 CPU writer 仍使用空前后资格与空 CUDA receipt，并明确 synthetic_cpu=True。
7. 真实 CPU writer 写出8个合成工件，其中7个输出由第8个 manifest 逐件 SHA 封口。写前 source/model/parent/ROOT/附加 HF/附加 LF/data 七种实际字节漂移均在 mkdir 前拒绝。第三个 CSV 真 writerows 返回后及 manifest 真 SHA 读取中分别注入 source/model/附加 LF/data，合计8种真实写后漂移均拒绝；前者不写 manifest，后者已写 manifest 后拒绝，所有失败产物原样保留，未伪装成功。

关键封口位置为 task11_howard_observations.py:728 在7输出后、manifest前 fresh verify_runtime，:731 在 manifest 真写出后再次 fresh verify_runtime。两处均传递初始 checked 与全部 additional pins，初始 pin 不得被附加 pin 替换。其前写入与正式逐态预测阶段另有 fresh verify_runtime，未改动这些源码。

## 原始实跑与自身失败

各 command.json 保存完整实际 env/pytest 命令、argv、工作目录、开始终止时间、退出码、墙钟、完整 stdout/stderr、逐件 before/after；同名 stdout.txt、stderr.txt 另保存原始文本。固定路径中的中文空格按 shell quoting 保留，未用大单 shell 参数写 JSON。

| 本人实际调用 | 退出码 | 终态 |
| --- | --- | --- |
| run_independent_cpu.py 83 | 0 | 83 passed in 41.41s；独审本人83.command.json |
| run_independent_cpu.py independent | 1 | 11 passed, 18 errors in 3.33s；独立追加IO病例.command.json |
| 首次诊断Float32求和.py | 1 | 自身诊断 line24 AssertionError；Float32诊断首轮补录.json |
| 修正自身诊断Float32求和.py | 0 | Float32求和根因实际证据.json 保存结构化实际终态 |
| run_independent_cpu.py independent2 | 0 | 29 passed in 39.06s；独立追加IO病例_修对照.command.json |

首轮追加29的18个错误均发生于同一独立夹具 setup，未走到这些 writer 用例。夹具错误假设 NumPy Float32 归约/独立除法与原 Polars 已存 Float32 归一值可用 rtol=2e-7 逐点比较，实际最大绝对差5.5879354e-9、最大相对差2.543978e-7。首次诊断又误设独立同 dtype 除法应 bitwise 相同，实际失败也完整补录。后续只修正自身 Float64 理想对照的容差 rtol=1e-6、atol=1e-9，候选源及原 frame_weight 严格原值比较没有放宽。独立诊断实证理想归一绝对差小于3e-8、原帧和对1偏差小于1e-6，属于跨实现 Float32 舍入，不判候选缺陷。诊断记录24路径含 pytest current alias，对应12个唯一 CSV，不声称24个独立病例。首次夹具、执行器、诊断脚本原件和全部失败临时目录保留。

所有三轮 pytest 守卫均记录 CUDA 初始化尝试0、cuda_initialized=false、项目隔离外 .pt/.parq/.parquet 打开拒绝计数0。83轮未调用 PT/Parquet 真实 loader，因此新增独立真实低层 IO 不能由83假读次数替代，最终29另有12/2实际读计数。

## 执行边界与后续许可

使用 requesting-code-review 与其完整 code-reviewer 要求执行独立 review，使用 verification-before-completion 要求先实跑再结论；尝试再分派只读静态复核因团队线程已满失败，此事实不作通过证据。自身夹具失败后使用 systematic-debugging 和 root-cause-tracing 查明跨实现舍入，只修自身档案夹具。不使用任何制定计划技能，不扩展任务12/13或其他模型。

CPU 固定环境为 CUDA_VISIBLE_DEVICES=''、OMP_NUM_THREADS=1、MKL_NUM_THREADS=1、PYTHONDONTWRITEBYTECODE=1、PYTEST_DISABLE_PLUGIN_AUTOLOAD=1，指定 PINN python -B，禁用 cacheprovider/logging，唯一 basetemp 均为本档案新增目录；TMP/TEMP/XDG/MPL/Torch扩展/Inductor/CUDA cache 显式指向项目既有 HowardLF 隔离缓存目录。完整精确命令见机器证据，而非可变聊天旧记录。

本报告只完成有界的任11独立 CPU 代码复核，可供父执行者决定下一 SOURCE90 普通冻结步骤。正式观察必须待本人五 HF 全部实际完训、best/final 十原件与完整自有资格齐备，并由父执行者按 OBS 自己六 SHA 活动 ROOT 门禁明确授权后执行。正式三模态指标、十态能源、安全性、内部真值、作者 exact 复现、GPU审计或全目标完成均不是本次结论。
