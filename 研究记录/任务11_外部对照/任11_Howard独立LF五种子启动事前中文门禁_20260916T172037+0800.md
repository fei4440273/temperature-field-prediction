# 任11 Howard独立LF五种子启动事前中文门禁

- 登记时间：2026-09-16T17:18:44+08:00；真实训练0/5，GPU优化尚未开始。
- 独立固定YAML：`研究记录/任务11_外部对照/Howard适配独立LF预算前登记.yaml`，SHA`0de2aa7b3e78d92d76ff90dfb2e9a1c6f5b4ba9f98ead462bd24e1f23dcc8c6c`。
- 35普通源码事前tar：`研究记录/任务11_外部对照/任11_Howard独立LF35普通源事前冻结_20260916T171319+0800.tar.gz`，SHA`42e1c376358f915e38625d9a197ae99606f24ff4af0e51a59e176fcba3d258bc`。
- 共同70真实源目录：`研究记录/任务11_外部对照/任11_Howard独立LF共同70真实源目录_20260916T171319+0800.json`，SHA`573550239566a5f1f0e7f6889fa40f99dde83379e7c2ebf7e89eae1c375da367`。
- 根封存前184项CPU证据：`研究记录/任务11_外部对照/任11_HowardLF封存前根独立184项CPU证据_20260916T172037+0800.json`；回归实际184 passed in18.70s、退出0。训练时长尚未发生，不编造。

## 结构和共同预算

Howard低保真仅Modified DeepONet teacher，没有dummy PH/QH或HF子网。branch1/trunk4、width128、depth4、latent128、LF末输出不激活、坐标固定缩放、Kelvin温度偏置只映射一次；参数100864。原LF子网可按严格完整状态迁移，但本LF准入绝不授权HF。

| 预先锁定项 | 每种子 | 五种子总上限 |
|---|---:|---:|
| 种子 | 固定0、1、2、3、4各独立 | 5 |
| LF训练／合法验证功率 | 60／10 | 同一折 |
| 每功率训练／验证采样点 | 8192／8192 | 不变化 |
| 每轮训练点／观测步／物理步 | 491520／60／1 | 不变化 |
| 每轮名义物理配点 | 256 | 不变化 |
| 最大LF轮次 | 2000 | 10000 |
| 最大训练点／优化步 | 983040000／122000 | 4915200000／610000 |
| 最大验证点消费 | 163840000 | 819200000 |
| 最大名义物理配点消费 | 512000 | 2560000 |

AdamW lr0.001、weight_decay1e-6、默认betas/eps完整锁YAML；物理原模式权重1，material_time采样，训练seed本人、验证seed10000。耐心200轮、min_delta=0.0001℃，提前停止不补2000。每个真单CUDA进程至多200轮，只恢复本人已提交最近完整阶段，禁止静默CPU回退/多卡/跨seed接续。

## 执行前证据与守门

35归档/现场/登记候选逐字SHA相同，普通唯一成员35/35；catalog仅采集70训练/验证仿真的真实行数/schema/SHA，与现场共同collect函数完全相同。模拟TEST仅已有manifest功率身份，不读取任何温度。catalog保留历史MLP用途字段以同函数核真，但绝不读取旧MLP权重。

在录0107尚未激活时，使用真实三SHA运行正常CLI54 --preflight-only已实际退出1，原因明确为缺Howard唯一独立ROOT门禁；seed0输出仍不存在。此为预期负门禁，不是训练失败。激活后须再次正常CPU预检返回0，才启动实际CUDA。

录0107仅LF独立整格：`TASK11_HOWARD_LF_GATE:v1; status=active; YAML_SHA256=0de2aa7b3e78d92d76ff90dfb2e9a1c6f5b4ba9f98ead462bd24e1f23dcc8c6c; TAR_SHA256=42e1c376358f915e38625d9a197ae99606f24ff4af0e51a59e176fcba3d258bc; CATALOG_SHA256=573550239566a5f1f0e7f6889fa40f99dde83379e7c2ebf7e89eae1c375da367`。不能借MLP旧门禁，不能在ROOT尾缀或重复录号下执行。

## 状态、收据和成本

每轮保存真实初始/最近/最佳/训练末模型、完整AdamW、CPU/CUDA/Python/NumPy四RNG、可训练状态、采样轮次及日志SHA。每200轮以内提交不可覆盖历史模型/状态/日志前缀＋连续收据；最终完整CPU资格需重算真实耐心/历史最佳、每个20参数61步×累计轮次、所有快照和收据SHA。

成本与新MLP同边界按本人epoch训练＋每轮验证＋完整阶段保存计；加载构建恢复、导出及源核验另列，总墙钟三项闭合。峰值每身份取真实会话max，不加不同seed峰值；收据后只读资格审计不混入训练成本。

## 原文适配边界和失败处理

本项目采用先LF预训练再以后另门禁三网全参数联合，是统一预算项目适配；不是Howard作者仓库移植、原文三网从零同时联合或原论文数据域精确复现。标量功率、SiC/Cu轴对称域、原名义平方物理目标、AdamW全参数decay与论文物理平均绝对误差/显式branch L2不同；共同LF预算不代表同LF起点、参数量或严格单因素。

封存后的源、模型构造、采样和budget不临时改；遇真实异常留现场记录再按台账处理，不以减少batch/去物理/换source挤过资格。纯CPU回归不替代真实CUDA及完整预算连续/分段位级对照。HF真实PH/QH、三网优化/正则和统一HF预算必须未来独立事前锁定，本行不给HF许可。

登记文本工具JS语法错误及无写入证据见`研究记录/任务11_外部对照/任11_Howard登记文本工具语法异常中文记录_20260916T172037+0800.md`。旧B0部署、MLP69及各LF/HF原件不变。所有写入只在本项目；旧TEST/仿真TEST研发温度禁读，真实新增测温仍0，任务11/总goal不标完成。
