# 任11新MLP合法三模态观察正式导出前中文审查

事前静态身份采集时间：2026-09-16T18:30:39.368776+08:00。本报告只验收观察代码的CPU回归、来源字节和预登记合同，**当前尚未执行正式CUDA观察**，不把合成夹具冒充真实模型或新实测。

## 一、实际代码审查范围

根最新六套回归实际退出0：415项通过，62.47秒。包括观察83项、Howard CPU初始化148项及相邻已冻结LF/数学/模型回归。实际核对79个普通源码原件，前后SHA完全一致，其中观察执行依赖72个。Python路径写入审计记录为空，CUDA隐藏；只能证明此次实际触发的Python路径事件，不能扩张为所有底层系统调用的全面隔离证明。

独立只读审查者task11_hf_init_p2_review另跑完整观察83项，实际退出0、38.71秒。其最终结论为当前CPU合成组件范围内没有新的P0/P1/P2：原四P2及HTML注释停用门禁P2已闭环。独审命令和审查来源均保留于[机器证据](任11_新MLP观察83独审与根415源码门禁前证据_20260916T183039+0800.json)，不把独审口述结果写成根亲自运行。首次调度127未启动Python，显式重试0后才引用该结论。

来源核验只读取源码、四原HF登记身份、十份best/final模型文件字节哈希；未反序列化PT、未打开温度列、未调用CUDA。实际72普通成员归档创建退出0，逐成员归档/现场SHA核对一致；无目录、链接、重复或额外成员。

## 二、固定只读观察合同

固定合法validation功率115.2、403、630.5 W，五seed各best与final，原Float32六输入新MLP工厂。合同规定每状态7272顶部点与752两环点，共8024点；拟十状态80240点。拟输出逐功率三模态90条、时间窗270条、顶部原生径向窗90条，以及完整逐点来源和七内容SHA清单。以上是**合同规定值，不是当前已取得的正式观察计数**。

顶部使用本人原frame_weight，在每统计范围内归一；Hot与Cold分别等点。差分按各功率各顶部半径或各环本人曲线首实际观测，不虚造t=0。绝对/差分p95先在各seed、功率、模态或窗口内按未加权经验分位计算，再对五seed作描述统计；偏差和峰值均保留符号。

时间窗固定为[0,30]、(30,100]、(100,200]秒；空窗全部指标null并给中文原因，绝不补零。顶部径向窗固定原生Float32米值对应[0,8]、(8,17]、(17,25]毫米；仅保留同源25毫米闭端点修复，不冒称任意采样端点已验证。

统计单位为五个本人模型seed，报告均值、ddof=1样本标准差、最小、最大与原五值，不把80240点当独立实验，不添加未预登记p值或安全阈值。原macro核对容差1e-4摄氏度，仍使用原Top1/环绝对0.2/环差分1选分，而不是训练ABS5。

## 三、独立登记与运行约束

新增固定观察YAML：`研究记录/任务11_外部对照/新MLP合法三模态观察导出前登记.yaml`。YAML由实际Python常量通过yaml.safe_dump生成，原数值类型经_strict_contract核验；不借旧MLP HF、Howard LF或CPU初始化许可。

新增源码归档：`研究记录/任务11_外部对照/新MLP合法三模态观察源码事前冻结.tar.gz`，SHA256：`592b230fa999ca151bc37c2734007c204fb19009b8af49708ed68cce9d6c5240`，72普通成员逐一匹配现场。旧HF67、聚合69与HowardLF35原登记和归档不改。

拟正式输出全新直属目录：`研究记录/任务11_外部对照/正式新MLP三模态合法观察_20260916T183039+0800`。只准单进程单卡CUDA，无CPU正式导出或多卡回退。新ROOT活动门禁之后才允许完整五HF来源资格和合法validation温度读取；整ROOT执行前后字节必须不变。预测后及写出前均重新完整核验五seed来源，任何漂移拒绝写出。

## 四、结论边界与异常记录

本次不训练、不优化、不重新选模、不重新计算能源与训练成本；不读取旧固定TEST或模拟TEST温度，不修改旧B0部署。当前新MLP名义能量标准通过0/10仍保留，不能用观测误差替代能量合格。HowardHF0/5、新增真实测温0，任务11及全计划仍未完成。

ROOT0108追加后已现场复核：录0101/0107原行保持，录0108唯一，Task11 JSON完整一致，四LF证据与观察72源码SHA一致。此前工具V8不提供structuredClone造成0毫秒编排失败，未执行apply_patch/Python、未写原台账；后续仅在工具内使用标准JSON深复制恢复。该异常不是Howard训练故障，旧失败记录不删除。

最终正式观察验收还须实际CLI57退出0、80240真实逐点及90/270/90记录、原macro容差、原件全SHA前后与独立真实数据四轮复核闭环。本报告不提前宣称上述项目已完成。
