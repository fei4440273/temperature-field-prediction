# 任11新MLP聚合前工具调度异常中文记录

登记时间：2026-09-16T16:54:22+08:00。以下为工具层未获得有效回执的调用，不是正式模型训练失败或新的温度结果。

## 已观察异常

1. 十能源标准库批次驱动首次与同命令重试，共两次调用均返回`failed to serialize JavaScript value: expected value at line 1 column 1`，没有可追踪会话句柄。当时只读ps未见该Python驱动，nvidia-smi计算进程为0，能源输出目录为0。
2. 聚合69源码清单的带断言Python查询未返回有效回执。随后用独立YAML读取、实际当前69文件SHA和69个普通TAR成员逐字SHA完成了同范围核验，三者一致；不以失败查询宣称通过。
3. 在ROOT0105激活前的正式聚合CLI探查与仅捕获子进程标准输出的诊断各一次，均未取得有效退出回执。实际输出目录仍不存在，但这不能证明正常CLI已进入门禁并返回预期拒绝；本次不把此两次探查计作门禁验收。

## 实际替代路径与边界

能源没有改冻结源码/参数/预算。根直接依次调用同一个冻结CLI52，十个seed/state规范全新目录均真实观察退出0，六实际工件SHA独立捕获，原始标准输出与精确命令见[十次执行回执](任11_新MLP十次独立CUDA原能源执行回执_20260916T165245+0800.json)，SHA`6585628c7c1d3d5b90628ae1a42480df75c81f0d0686399f57e659b85e1f2103`。十能源前后五完整资格对象相等，全部235训练原件SHA不变。

源码归档使用普通TAR工具并重新读取各成员字节SHA，当前69源与后置归档及原登记67源完全一致；新两聚合source/tests的真实SHA亦核验。ROOT0105和外目录SHA仍须在正式聚合前激活；合成72/72回归已通过，真实CPU聚合尚未执行。未确定工具内部序列化异常的根因，不猜测成CUDA故障或模型误差。

所有记录、缓存和工件仅在本项目。没有覆盖旧结果、读取旧TEST/仿真TEST温度、启动Howard或伪造任何失败指标。
