# 任10人为双层板：仅50,000 W/m² CPU数值入场

事前源码tar SHA256：`479049766955ef5a8bdbaa2d5eb42be4989f6b49366328b4ab3454cf1f29f742`；专属配置SHA256：`0d85a34f12e83c37d287943728136dfd1d56d37cf089bd374568cd397ea14c13`。测试折42,000/58,000 W/m²的隐藏温度未生成或打开；未运行任10任何主模型/基线训练。

细两级固定时刻/深度最大差 `0.097533287882 ℃`，门槛严格小于 `0.1 ℃`；单步每平方米热余额最大 `9.36968717724e-07 W/m²`。人为输入热功率为 `141.371669412 W`，不是实际装置激光功率。200秒参考顶部 `32.685785536 ℃`、界面通量 `50000.000000 W/m²`、跳温 `5.000000 ℃`；失配LF200秒顶部 `31.185785536 ℃`。

仅 `HF_允许探针/训练_50000.npz` 提供三个允许温度位置和14个前登记时刻；两个LF归档为完整粗网格场。1秒与5秒不在2秒LF时间网格，CSV对应LF列留空，不伪造插值。完整HF内部参考不入训练/验证目录，正式隐藏误差须模型另行同板重训、合法验证及检查点SHA锁后才可计算。
