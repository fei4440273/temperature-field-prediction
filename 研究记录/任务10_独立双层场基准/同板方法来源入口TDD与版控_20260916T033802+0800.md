# 任10同一维板F1/F2/F3：只合法来源入口TDD与版控

## 版本/来源

本文件只记独立**模型输入合同**，并不是方法预算、训练、模型评价或任务10内部场比较原件。此前一维人为九折/几何/热阻/探针登记SHA256为`0d85a34f12e83c37d287943728136dfd1d56d37cf089bd374568cd397ea14c13`；执行前六源正式CPU参考tar SHA256为`368f0f8458c994101bd1d20c241f61f8e64d8bedb0e74b3f733d7369e7a01c2f`，不改其成员。台账后正式受限源`九热流受限数值源_台账确认后_20260916T032402+0800/`的逐路径26原件清单SHA256为`b59bc80c678faa29224e564dc3f4dd7d61903fc59d6c7e6c2274e1a59651bedb`，九热流CSV SHA256为`e6f538199e815014f428f1e53b92284d39c70968ef15d79e882e0e4aff99e29d`；首次早于主台账的预运行只留证，不作此入口的正式来源。

新增独立来源源码`src/sic_cu/eval/task10_plate_training_source.py` SHA256为`3b75b92bc73cc7c207bdc901086bfbdb7bdc4d322db9a7d4aa78e317fc6adf8d`；新增回归`tests/test_task10_plate_training_source.py` SHA256为`5530a6b2ece7cd33c64ce3552fe4bc1d5d5cbe6ffd1c305dbe53edae7128cc81`。只含这两文件的新增入口源码tar为`同板方法来源入口源码冻结_20260916T033802+0800.tar.gz`，SHA256 `7a8165f8f988e8df717566a3a77e51064384ec0d276015c56853bb2c964c80a6`，两成员已只读核对。该tar**不是**包含未来架构/优化器/训练预算/训练程序的正式方法训练快照；未来真实训练须另封版本并记主台账。

## 来源合同

统一特征逐行`[顶面以下深度m, 时间s, 人为顶部面热通量W/m², 材料ID]`，标签K；不是原圆柱`[r,z,t,整束激光功率,材料ID]`。HF可开放的训练/验证一维顶部、Cu内13/16mm三探针每档14时刻各`(42,4)`坐标及`(42,1)`温度，共训练五档210点、验证两档84点。F1只取训练探针且强制拒任一LF；F2/F3各明确固定**一种**预登记LF来源，只可取训练五热流24/11双层板、2秒的完整粗网格每档`(3535,4)`坐标及`(3535,1)`标签（每种LF条件五档17675点），分别可在同物理或人为0.7倍Rc配对条件独立重训。验证view永远只开放30/70千W/m²三探针，无任何验证LF。禁止直接使用旧圆柱E0权重推理；E0若要同板基准须另设计独立DeepONet重训入口，F4没有合规增强方法原件。

训练来源构造器**要求训练前已独立确认的机器源清单SHA256**，且每次合法温度读取前复核清单字节SHA及项目内实路径，拒绝后续内容漂移或指向项目外的符号链接；原受限view另复核登记SHA、档案逐路径SHA、字段白名单及LF网格ZIP头。新增入口进一步验证LF一维网格节点深度/SiC-Cu材料序列/已登记时间轴。真实来源仅只读取合法批次：F1训练5/验证2/无LF；F2同物理LF训练5/验证2；F3 Rc0.7 LF训练5/验证2，全部形状和材料轴吻合。此处F2/F3各取不同LF的例子**只验证软件入口形状，不构成F2与F3的单PDE贡献对照**：正式对照必须F2/F3取**同一种LF**、相同seed及预算分别重新训练，只让是否计算PDE变化；同物理与Rc0.7两来源的效果属于另一个单独的LF失配对照，不得混合两种差异求一个收益率。该真实来源形状回归**不是**优化更新或内部HF参考验证。数值隐藏42/58千W/m²不在训练/验证来源任何折，`open_full_hf`明确拒绝；全过程不解码隐藏温度、不使用旧`test_Data`温度、不占GPU。

TDD原七条新入口fixture测试先仅因模块不存在RED、后GREEN；追加项目内清单symlink逃逸新例先RED（旧检查会先追符号链接）、仅在新增入口复用已有canonical实路径校验后GREEN。最后fresh聚焦`tests/test_task10_plate_training_source.py`、`tests/test_task10_plate_benchmark.py`、`tests/test_two_layer_fvm.py`、`tests/test_physics.py`共53/53通过；`TMPDIR`与`--basetemp`只在本项目内，旧FVM、脚本22及原已封存源未改。

## 严格限界/下一动作

源合同能挡正规训练入口、清单漂移和常见方法偷读完整HF，**仍然不是OS读取权限隔离**；任意恶意Python代码绕过入口可直接访问工厂目录，真实trainer须另做静态数据入口审计及源码快照。清单SHA不能由方法运行时临时拿当前文件的SHA冒充事前固定；上述形状烟测运行时计算源SHA仅用于只读验证，没有开始训练。F1已知同板物理的PDE/界面边界损失、F2“真正无PDE”、F3同板PDE与两LF比较语义均**尚无模型或动态门禁实现**；来源入口不能替训练日志自证、不能产生隐藏位置或能量模型分数。

下一合法动作：训练前新登记三方法及同板重训DeepONet基线的架构/共同随机种子/相同且有限的优化与候选预算，固定上述受限清单SHA和每次训练源码SHA；F1仅合法HF探针+公开已知物理，F2/F3须先在**同物理LF、同seed、同预算**下重训配对核单PDE差异，再各自另在**Rc0.7 LF、同seed、同预算**下重训，失配来源只跨相同方法比较；F2零PDE须运行审核。全部候选/早停仅用合法30/70千虚拟探针；每次实际训练/合法验证和采用模型SHA锁定后，才让独立后验数值评价器按逐热流网格/数组格式头/源SHA打开全部内部HF以及42/58千隐藏温度。未取得原装置HF实验内部真值，任何同板数值结论不得改写原圆柱乙冻结结论。
