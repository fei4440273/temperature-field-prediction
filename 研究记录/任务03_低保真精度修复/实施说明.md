# 任-03：LF拟合与HF收益传递预登记

预登记2026-09-15T18:35:46+08:00，尚无任-03新训练指标。原LF由V4 seed0清单锁定，旧最佳LF epoch178。当前LF采样已按每个功率、Cu/SiC、三个时间段均衡，但层内全部空间节点随机抽取，没有显式径向/轴向覆盖，故空间采样分支有触发依据；沿用原时间层端点用于两臂相同配额，新合法评价窗口单独使用`[0,30]`、`(30,100]`、`(100,200]`。

共同起点、学习率、300轮上限、60个仿真训练功率、每功率8192点和每轮491520点相同。控制臂沿用原材料/时间采样，候选仅增加层内空间平衡；没有额外扩大网络或改温度无量纲化。旧LF检查点缺优化器，两臂均重建相同AdamW，并记录真实最佳及末态。旧LF原名为`deeponet_pinn`，但HF名义物理参数已经显示工程能量失配；两臂共同关闭该名义HF物理优化步，只使用对应仿真标签，不能据此把空间分支独立归因于旧完整LF训练。LF选择仅用10个LF合法验证功率，逐材料/功率/窗口的节点及体积RMSE用真实全场仿真温度核算。

LF验证候选确定后，以同一历史HF校正器权重、相同12个HF训练功率及原S0排程分别接旧LF与新LF，最多各300轮；HF组合选择依据3个合法HF验证功率。旧LF控制的HF臂同样重新初始化校正器并重新训练，不把任-02现成不同时间源码/阶段产物冒充同源对照。任何LF改善但HF不改善只记录，不自动替换。禁止读取`test_Data`旧测试温度、改变全局60/10/10或12/3/3划分，以及把名义物理能量审核当内部真实物理合格证据。
