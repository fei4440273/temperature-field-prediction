# V1版本说明

日期：2026-09-16。

V1保存本项目截至任-11执行结束、后续实验暂停时的代码和已有结果。V1是版本名称，不表示新模型已经通过精度与物理验收，也不表示总任务全部完成。

## 版本位置

仓库：[temperature-field-prediction](https://github.com/fei4440273/temperature-field-prediction)。

本次新增`V1`分支和`V1`标签，不覆盖已有`main`，不改写历史提交。先上传当前项目，再补充任务总结；最终标签包含任务总结。

## 上传范围

| 上传到GitHub | 保留在本地，不上传 |
| --- | --- |
| 当前源码、脚本、单元测试、配置、已有文档与任务台账 | 原始数据、处理后的温度数据、模型检查点 |
| 正式结果JSON、汇总CSV、中文验收报告与诊断报告 | 重复测试目录、缓存、锁文件 |
| 小型源码固定归档与审查证据 | 大型逐点预测、NPZ/VTK输出、两份Howard失败现场完整归档 |

不上传的内容仍保留在本项目内，没有删除。报告中记录的本地路径、文件大小与SHA-256保持原样；部分文件因此不能只靠GitHub副本直接重放。上传代码和汇总报告不等于上传了完整数据与模型。

Howard两个失败现场大归档的本地身份如下，GitHub收录对应诊断与逐文件核对记录：

| 文件 | 大小/字节 | SHA-256 |
| --- | ---: | --- |
| `研究记录/任务11_外部对照/Howard_HF_seed2第304轮数值失败完整普通字节归档_20260916T205117+0800.tar.gz` | 87312127 | `61f681baced196bbb94587a16ec4506aaf672096eb7cbfe67259c31b547f100e` |
| `研究记录/任务11_外部对照/Howard_HF_seed4第210轮数值失败完整普通字节归档_20260916T212312+0800.tar.gz` | 103291013 | `6b0934dfd7a23dfb8067c111404817cf03affd0c0b6c9d0d2199ec1b6de3bd6e` |

## 安装补充

在Python 3.9或更高版本的独立环境内，从项目根目录运行：

```bash
python -m pip install -e '.[test]'
python -m pip install -r requirements-v1-extra.txt
```

新增的固定Ridge对照使用`scikit-learn==1.6.1`。原`pyproject.toml`和`requirements.txt`已经被实验记录保存了文件校验值，因此本次不修改原件，用补充文件列出这项依赖。

模型训练、数据处理和原结果重放还需要本地数据与检查点；安装成功本身不证明这些结果可直接重放。

原README中的全套测试命令还依赖本地数据；部分历史审查测试写有本机PINN解释器路径。仅克隆仓库不能保证全套测试运行，本次上传不宣称全套测试通过。

## 结果入口

- [此前的详细结果汇总](任11执行收尾与已完成任务结果汇总_20260916.md)
- [任务总台账](多保真DeepONet预测精度优化总计划与执行台账.md)
- [当前任务状态](研究记录/总任务状态.json)
- [当前接续状态](研究记录/当前接续状态.md)

任-12、任-13及新增优化实验保持暂停。此次只进行版本上传与已有结果总结。
