# 多保真DeepONet连续联合训练与结果图册

本次改动只做两件事：**连续8000轮高低保真联合训练；生成可以直接查看的实验—预测结果图册。** 不继续开展其他模型对比，也不要求补充内部测温数据。

## 立即运行

在项目原有GPU环境、保有规范化数据的项目根目录运行：

```bash
python scripts/联合训练8000轮.py --device cuda
```

使用现有 `data/processed` 数据，不移动训练、验证或测试集。程序要求固定的高保真12/3/3与低保真60/10/10功率划分。没有完整规范化数据时会报告实际缺失路径，不能拿历史小归档或合成数据代替。

## 本次训练的明确约定

| 项目 | 本次行为 |
|---|---|
| 轮数 | 同一个循环从1到8000，没有3000+2000+3000之类的阶段 |
| 训练数据 | 每个优化步骤同时使用完整仿真场的分层抽样、顶部实验及热端/冷端实测 |
| 网络参数 | 所有网络参数从第1轮共同参与；不先做低保真预训练，不冻结后再解冻 |
| 一轮定义 | 遍历一次全部高保真训练顶部记录；每批配对抽取全部仿真功率/材料以及全部训练环温工况 |
| 优化器 | 同一AdamW优化器，一次合并损失、一次反向传播、一次参数更新 |
| 学习率 | 8000轮内连续平滑降低；不改变训练阶段或数据来源 |
| 停止 | 没有早停；只有中断、硬件或数值异常会停止并保留记录 |
| 模型保存 | 每轮更新最近模型，同时保存验证最佳模型；第8000轮另存最终模型 |
| 测试 | 完成8000轮、按验证集锁定模型后，才读取测试集出图 |
| 原结果 | 不覆盖V1、B0及原有冻结文件 |

模型仍使用DeepONet的功率分支、时空分支及材料感知温度校正；没有新增LSTM、扩散模型或其他比较任务。程序保留两个温度输出以区分不同保真度，但不是分开训练两阶段。仿真标签拟合仿真输出，实验标签拟合实验校正输出，物理约束作用于实验校正后的完整场。

## 水冷条件已经分开

| 物理量 | 仿真 | 实验校正预测 |
|---|---:|---:|
| 外圆水冷温度 | 22.000 ℃ | 25.000 ℃ |
| 初始温度 | 沿用原仿真设置 | 默认沿用已有初温，未把水冷25.000 ℃当作初温 |
| 环境温度 | 保留原配置 | 保留原配置，未因水温改变而改环境温度 |

采用边界损失，不用常数覆写后再将零梯度当水冷通量。也不把全部实验温度或预测温度统一加减3.000 ℃。接触热阻作为一个正值参数与网络共同拟合；发射率、吸收率、光斑等仍沿用当前工程配置。本轮不再要求额外的内部温度观测。三维结果直接标为“预测温度”。

## 图册在哪里

运行结束打开输出目录中的 **`结果总览.html`**。所有标题、图例、说明为中文。温度与误差展示保留小数点后三位；原始数值计算和机器检查点不提前舍入。

| 图件 | 具体内容 |
|---|---|
| 表面逐时刻并排图 | 测试集每个实测时刻：实验温度、预测温度、预测减实验误差 |
| 表面动画 | 按真实红外帧时刻播放上述并排对比 |
| 表面径向曲线 | 每个实测时刻的实验点与预测曲线 |
| 热端、冷端曲线 | 每个测试功率的全部实测时刻，实验与预测直接叠加，并附误差曲线 |
| 三维温度动画 | 0—200 s的SiC–Cu半剖温度场，分别输出实验校正预测与仿真分支预测 |
| 损失变化图 | 仿真、实验、物理各项及总损失，横轴为1—8000轮 |
| 验证误差图 | 顶部、热端、冷端及综合选择分数随轮次变化 |
| 接触热阻曲线 | 横轴为训练轮次，纵轴用10⁻⁵ m²·K/W，避免舍入为0.000 |
| 体场数值 | 轴对称截面温度序列NPZ，与三维动画来自同一模型 |

表面原始CSV默认读取 `data/test_Data/topdata`。存在原始像素时直接在原始像素坐标预测；只有规范化径向数据时，图题明确写“实验环平均数据还原”，不冒充原始热像。表面实验和预测使用相同色标，三维动画全时间使用固定色标。没有实验的时间不会伪造实验帧。

## 中断与继续

```bash
python scripts/联合训练8000轮.py --device cuda --resume "研究记录/实际运行目录/最近模型.pt"
```

这是同一次8000轮训练的恢复，不是新增训练阶段。配置和划分不一致时拒绝拼接。若第8000轮后只需重新出图：

```bash
python scripts/重新生成结果图册.py --run "研究记录/实际运行目录" --device cuda
```

使用原项目的Python环境，依赖PyTorch、NumPy、Polars、PyYAML、Matplotlib和Pillow。中文字体自动查找；系统没有中文字体时会在长训练开始前提示，Ubuntu可安装 `fonts-noto-cjk`。不附带任何字体文件。

## 实施记录

每次运行自动更新 `实施记录.md`，包括时间、实际修改、完成轮数、顶部/热端/冷端和综合指标。`训练记录.jsonl`保留未舍入数值。报告中的验证误差不是三维内部实测误差；这里不要求提供内部真值，也不虚构内部“实验对比图”。

本次交付已完成代码与本地单元测试，**没有在当前会话中执行真实8000轮训练**。本会话没有仓库完整训练数据、检查点与GPU；这不意味着用户未提供过数据。请在原来保有数据的训练项目中执行上述命令。未产生的新误差和新动画不能填写为已有结果。
