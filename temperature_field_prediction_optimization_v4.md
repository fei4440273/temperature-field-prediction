# SiC–Cu 多保真温度场预测：优化与投稿准备执行方案 V4

> 用途：交给 Codex，在现有项目上增量实施；不是从零重写工程。  
> 日期：2026-09-09。  
> 核对仓库：`fei4440273/temperature-field-prediction`。  
> 核对提交：`bbe563e05ee65e28362ccab04d91773896d8d0c4`。本次查询时 main 仍指向该提交。  
> 本轮目标：围绕一个轻量精度增强假设，补齐可归因的多保真实验、全域数值验证和独立实测证据。  
> 阅读边界：已核对仓库固定协议、多保真模型关键代码、验证检查点清单，并阅读上一版 V3。没有重跑原始数据、训练或检查点；本文所有新实验均为待实施建议。

## 0. 先执行哪件事

**第一项任务不是训练新网络，而是导出现有 DeepONet 多保真模型在训练集、验证集上的诊断报告。**

第一批必须得到四份结果：

1. Top/Hot/Cold 的逐功率、逐时间窗误差及有符号偏差。
2. 低保真分支在多保真微调前后，仿真验证集上的 SiC/Cu 全域误差。
3. 每种损失的有效优化步数、梯度量级，以及共享参数上的梯度夹角。
4. 时间表头、初始温度、温升参考时刻的语义核对。

随后只开发一个主候选：**低保真热响应特征增强的高保真校正器**。保留低保真 DeepONet，不让 PRC、GNO、FNO、扩散模型同时进入本轮研发。

关键变化不是再给算法起简称，而是通过对照回答：

> 利用完整仿真场提取的热响应时间特征，是否比普通时间输入或通用时间编码，更能帮助少量实验校正真实瞬态温度？

**这只是研究假设，不能在验证前宣布能提升精度，也不能宣称首次提出。** 方法名称仍使用 DeepONet-PINN 及其明确的实验变体；多保真与跨材料传热是各方案共同的工程条件，不塞进方法简称。

---

## 1. 为什么本轮这样收敛

### 1.1 已有结果的正确定位

仓库的冻结测试报告列出：DeepONet-PINN 五种子综合 RMSE 为 `1.9600 ± 0.2743 ℃`，MLP-PINN 为 `2.4361 ± 0.5547 ℃`，硬表面引导为 `3.1736 ± 0.0705 ℃`。PRC-AT 仅有 seed 0，尚未超过 DeepONet 对应 seed 0。[R1]

这些是已有观测位置上的宏平均指标，不是内部三维体场误差。本轮不以把旧测试集的 1.9600 改成更小数字作为调参目标。

DeepONet 五个检查点记录的验证选择分数分别是：

```text
seed 0: 2.0187304291
seed 1: 1.9540362036
seed 2: 1.9266398083
seed 3: 2.0091985952
seed 4: 2.0994066072
```

其均值、样本标准差为 `2.0016023287 ± 0.0667058238 ℃`。[R2] 这是验证选择分数，不是测试 RMSE；如果预处理、有效训练配置或评价口径改变，必须重新建立基线，不能直接拿它作新实验分母。

### 1.2 不再返工已完成内容

保留连续坐标直查、现有解析器、材料标签、物理损失、冻结发布机制、直接预测接口和导出器。现有代码已经支持 `fidelity="low"/"high"`，不能把“补上低保真分支”再次当成新工作。[R3]

V3 提出的排查方向仍有效，但 V4 增加了：具体试验编号、训练预算约束、时间特征拟合办法、数据效率子集、全域参考基准、独立补充实验与投稿验收要求。

---

## 2. 不变的数据协议

### 2.1 高保真

| 角色 | 功率 / W |
|---|---|
| 训练，12 个 | 55、216.8、254.5、257、309、364.3、430、494.2、532、558.5、593.5、729 |
| 验证，3 个 | 115.2、403、630.5 |
| 旧固定测试，3 个 | 169、339、634 |

以上来自现有协议。[R4] 同一功率 Top/Hot/Cold 始终一起划分。新方案不恢复 LOPO、LOGO、循环交叉验证；训练集内部的数据量消融也不是交叉验证。

### 2.2 低保真

```yaml
simulation_validation_w: [90, 170, 250, 330, 410, 490, 570, 650, 730, 800]
simulation_test_w: [50, 130, 210, 290, 370, 450, 530, 610, 690, 770]
simulation_train_w: 10~800 W 步长10 W，扣除上面两组后的60个功率
```

完整 SiC/Cu 体场必须参与低保真训练。允许分层抽样，不允许只保留仿真顶面。

高保真和低保真是两个保真度下的工况隔离协议，不要把二者笼统说成所有意义上统计独立。固定几何相同、邻近功率响应相关，是这个代理任务的正常属性；独立实验批次、未使用标签和功率不重叠是不同概念。

### 2.3 旧测试结果的使用边界

仓库协议已经披露169/339/634 W曾被作为验证数据查看。[R4] 本轮又受旧测试复盘启发，因此是新的研发轮次。规定：

- 原发布目录只读，不删除、不覆盖、不重新解释为盲测。
- 新代码开发、损失选择、时间字典拟合、噪声尺度估计全部不读测试温度。
- 不给634 W、5 s等旧测试实例设置特例或权重。
- 新模型冻结后在旧测试上的结果只能标为历史回归/探索性评价，不能重新称为从未接触的盲测。
- 保留当前划分即可，不通过重命名文件或把测试集挪入训练集“解决”历史暴露。

真正新增的独立实验另存 `prospective_test/`，在模型与分析协议冻结后评价；没有新增实验也能推进软件与验证研究，但不能扩大结论。

---

## 3. 唯一主建模路径：低保真完整场 + 高保真校正 + PINN

令 `x=(r,z,t,P,m)`，其中材料标签 m=0 为Cu、m=1为SiC，以代码现有约定为准。

\[
\widehat T_{LF}=F_\theta(x),\qquad
\widehat T_{HF}=\widehat T_{LF}+C_\omega(x,\widehat T_{LF},\psi(t)).
\]

基线没有额外的 \(\psi\)，新候选加入少量热响应特征。

### 3.1 各类信息的职责

| 信息 | 作用对象 | 禁止事项 |
|---|---|---|
| LF完整仿真温度 | 低保真分支 \(T_{LF}\) | 不把最终HF场强制拟合成仿真 |
| HF Top/Hot/Cold | 最终高保真输出 \(T_{HF}\) | 不只训练Top而声称全面融合 |
| SiC/Cu方程及边界 | 最终高保真输出 \(T_{HF}\) | 不只约束LF而放任校正项 |
| LF响应时间特征 | HF校正器输入 | 不用验证/测试温度拟合特征 |

默认损失为：

\[
L=\lambda_L L_{sim}(\widehat T_{LF},T_{sim})+
\lambda_T L_{Top}(\widehat T_{HF})+
\lambda_H L_{Hot}(\widehat T_{HF})+
\lambda_C L_{Cold}(\widehat T_{HF})+
\lambda_\Delta L_{\Delta T}+\lambda_{phy}L_{phy}(\widehat T_{HF}).
\]

各分项使用当前可追溯的无量纲化和工况权重，不把原始不同量纲残差直接相加。初版保持现有物理参数和正则策略，不同时改变未知边界参数、特征编码、损失权重和网络深度。

### 3.2 三阶段

1. **LF预训练**：60个仿真训练功率；仿真验证集选检查点。复用已审计的合规检查点，不重复无必要训练。
2. **HF校正**：固定LF参数，拟合校正器；LF前向与坐标导数必须保留。冻结的是模型参数，不是输入求导图，不能把整个LF前向包在 `no_grad` 里后仍声称PDE导数完整。
3. **可选联合微调**：只有预登记配置允许时解冻；仿真损失仍只监督LF输出，保留两材料消费量和LF验证误差日志。

初筛先比较相同LF检查点上的校正器，避免不同LF误差混进新机制贡献。五种子正式对比使用同一seed对应的同一LF来源，并记录LF SHA-256。

### 3.3 物理条件保持正确但不再次作为宣传中心

沿用原坐标约定：顶面 z=0，SiC底面 z=-0.012 m，Cu底面 z=-0.0175 m。两域分别求导，在界面使用一致法向处理热流与温差关系。

内部训练仍为开尔文，显示和误差为摄氏度。辐射项必须用绝对温度。已有22 ℃初温、水冷外圆、材料参数等不再要求用户重复提供；要核对的是实际测量基准与已有约定是否矛盾，而不是重新猜物理参数。

---

## 4. 第一张任务单：D0诊断，不训练新模型

建议新增 `scripts/07_export_development_diagnostics.py`。以下文件名和新命令均为待实现接口，不代表仓库当前已经具备。

### 4.1 输入与输出

输入：合规LF/HF检查点、train/validation、配置快照。**没有test选项。**

输出目录：`reports/development_v4/diagnostics/`。

| 文件 | 内容 | 用来决定什么 |
|---|---|---|
| `validation_by_power_modality.csv` | 每功率Top/Hot/Cold RMSE、MAE、偏差、温升误差 | 定位共同偏差，而非看综合值抵消 |
| `validation_by_time.csv` | `[0,30]`、`(30,100]`、`(100,200]`；每窗实际样本量 | 是否存在快/慢过程拟合差异 |
| `validation_by_radius.csv` | Top中心/中部/外圈，边界固定为0/8/17/25 mm | 峰值与空间分布是否各有误差 |
| `lf_retention.csv` | LF预训练与HF微调后在LF validation上的SiC/Cu误差 | 联合训练是否破坏LF基础 |
| `initial_state_audit.csv` | 每条曲线真实首时刻、首温、温升参考、已知初态 | 是否存在时间或零点冲突 |
| `gradient_diagnostics.json` | 梯度范数、夹角、记录时的原始/加权loss、参数组 | 物理更新与观测是否冲突 |
| `physics_validation.json` | 独立配点上的两域PDE/边界/界面、能量检查 | 观测好是否牺牲物理 |
| `run_provenance.json` | 有效CLI、代码哈希、LF来源、数据指纹、预算 | 是否可重复 |

径向区间仅为预登记分析窗口，不是材料新边界。窗内无观测输出null和原因，不能延长最后一帧制造真值。

### 4.2 采样与梯度诊断

使用固定训练诊断批次，不通过测试残差选点。对同一模型状态分别计算IR、Hot、Cold、LF和physics梯度；无公共可训练参数时，梯度夹角记为“不适用”，不能填0造成错误解释。

- 参数组：LF分支、HF校正器；另外标出确实共享的参数。
- 初步每20次更新记录一次、最多10个诊断时点；这是控制开销的配置，不是理论要求。
- 诊断采用同一状态和同一批次，不能比较不同优化步的梯度。
- 不把梯度量级大直接称为坏事；同时观察方向、最终验证与物理残差。

### 4.3 初态问题的处理准则

初始温度不同、传感器偏置和同步误差可能混淆。必须先核对训练/验证记录，不根据旧测试某个固定误差直接改初温。

仅在训练证据支持且有合理校准依据时，引入跨功率共享、受约束的传感器偏置参数。默认关闭。禁止每个验证/测试功率自由拟合一个偏置，禁止删除误差大的首点、用测试首帧校准或减去测试平均误差。

模型推理契约仍是给定功率和预先约定初态，不额外读取新工况的历史实测温度。若将来把实测初态作为新输入，需要单独定义新任务，对所有基线一致开放，不能混报。

### 4.4 D0通过条件

同一检查点改变评估批大小时结果仅有浮点级差异；全部统计均可追溯到训练/验证来源；冻结LF仍具有坐标导数；空窗口、材料分组和重复环温聚合测试通过。这里不要求先获得更好的精度。

---

## 5. 第二张任务单：建立本轮计算基线B0

### 5.1 先区分“修复”和“方法创新”

若D0发现单位、时间、评价或求导错误，先修复并让所有方法共享修复，然后重建B0。修复收益不单独包装成新算法贡献。

如果没有发现影响训练的错误，现有可追溯检查点可以作为基线，不需要再跑所有MLP/LSTM/POD/GNO。

### 5.2 训练排程只有在诊断支持时调整

当前实现每轮多次数据更新后再做物理更新，V3已经指出这一点。[R5] 默认不宣布它必然有问题。

如果诊断显示物理梯度被频繁数据更新抵消，可设置一个排程对照：

```yaml
schedule_control:
  reference: epoch_end_physics
  candidate: periodic_mixed_step
  physics_every_data_steps: 4
  keep_total_physics_evaluations_comparable: true
```

数字4只是初始工程候选，不自动最优。对照固定相同数据与物理计算预算；若小批配点无法保持预算，则增加一个匹配计算量的对照，并报告实际差异。

混合步中将所需loss放在同一次反向或正确梯度累积后更新。考虑高阶自动微分、DDP图同步和显存，不能把跨rank不一致的条件分支放入不同步的优化路径。

只运行seed0排程先导；没有验证依据不扩大。排程选择冻结后，下文所有算法对照使用相同排程。梯度平衡已有文献，属于训练机制参照，不是本文自动拥有的新创新。[E1]

### 5.3 基线锁定产物

`baseline_manifest.yaml` 至少包含：数据/划分/解析物理哈希、有效CLI、源码归档哈希、LF/HF检查点哈希、seed、可训练参数数、总参数数、训练阶段步数、配点数和验证选择方式。

历史发布的dirty commit需要配套实际源码或补丁归档；仅把新提交号写进旧训练产物，不能证明当时运行了该版本。[R6]

---

## 6. 第三张任务单：只试一个精度增强机制

### 6.1 修改位置

当前HF校正器已接受 `[r,z,t,P,material,T_LF]`。[R3] 新增可配置时间特征模块：

```text
src/sic_cu/models/thermal_response_features.py       # 新增
src/sic_cu/models/multifidelity.py                  # 扩展，不改默认输出
src/sic_cu/data/lf_response_dictionary.py            # 新增
scripts/07_fit_lf_response_dictionary.py            # 新增
```

外部模型名称暂不变化，实验记录用 `feature_mode=none/fixed_response/lf_fitted_response` 区分。

### 6.2 新模型

\[
\psi(t)=\left[1-e^{-t/\tau_1},\ldots,1-e^{-t/\tau_J}\right],
\]

\[
\widehat T_{HF}=\widehat T_{LF}+C_\omega(\widetilde r,\widetilde z,\widetilde t,\widetilde P,m,\widetilde T_{LF},\psi(t)).
\]

保留原有时间t输入，因此不仅能表示指数形式。**本轮只加4个响应特征；不同时加Fourier、Attention、时间导数、空间梯度或新的门控。** 新输入对损失可微；不将整个预测场限制为4个模式。

原有初值与边界开关保持与B0一致。响应特征本身在t=0为0，不意味着普通校正器必然满足初值；仍需原有一致的初值约束。避免重复叠加初值包络或边界覆写。

### 6.3 时间尺度如何仅从LF获得

使用全部60个LF训练功率；每功率从SiC/Cu各固定采32个节点，覆盖中心、内部、近边界。采样种子与节点清单冻结。仅用实际仿真时间，不读取LF测试、HF温度或HF稳定时间标签。

对抽样温升曲线拟合：

\[
Y_\ell(t)=T_\ell(t)-T_0\approx\sum_{j=1}^{J}a_{\ell j}(1-e^{-t/\tau_j}).
\]

- \(\ell\)表示一个功率与位置的曲线；每条曲线有自己的幅值，所有曲线共享少量时间尺度。
- 两材料分别使用LF训练数据计算的温升尺度归一化，再对功率、材料等权，防止高温SiC完全主导字典。
- 幅值允许正负，不把每个线性模式都当成独立非负热量。
- 固定tau时幅值用带微小岭正则的线性最小二乘求解；更新tau可用变投影/交替最小二乘。
- 使用float64拟合小字典，J=4，初值 `[2,10,50,200] s`。这是依据时间采样范围设置的数值起点，不是实测材料时间常数。
- 正有序参数化：`tau = tau_floor + cumsum(softplus(raw))`；`tau_floor`由非零时间分辨率给出数值下界并写入元数据，不宣称是真实最短物理过程。
- 初版限制拟合次数和初始化次数；建议最多2组预登记初值。先看LF validation的曲线重建与条件数，不能以HF测试表现选字典。
- 记录饱和快模态、慢模态幅值/时间尺度补偿、字典条件数与分材料重建误差。明显不可辨认的模式不作为“识别出的物理模态”解释。
- 如果4个共享时间尺度不能稳定拟合训练曲线，记录失败，优先保留固定特征对照；J=8属于单独备选实验，不立即无限增加模式。

最终校正器只使用tau，不需要输出每条LF曲线的线性幅值。低保真DeepONet仍是完整场主干，字典只是辅助输入；这减少额外空间网络与高阶导数负担。

### 6.4 工程实现细节

- `psi = -torch.expm1(-t / tau)`；tau注册为buffer，随检查点保存。
- 三个对照具有相同坐标、材料、低保真温度输入与原有预处理。
- 一开始冻结LF与时间字典，仅训练HF校正器。
- 不把 `T_LF` 或t特征随意 `detach`，避免PDE求导缺项；冻结参数仍应保留输入导数。
- 如果联合微调LF，时间字典仍默认冻结；否则需要独立试验解释是否仍然由LF决定。
- J=4新增输入只轻微增加第一层参数，但必须报告；固定响应与LF拟合响应必须完全等结构、等参数量。
- 无特征模式兼容旧检查点，或显式迁移版本；不能静默加载失败后回退到插值。
- 使用训练/验证配点检查0、2、5、30、100、200 s处的一阶/二阶导数有限。

### 6.5 最小消融，不允许少做关键对照

| 编号 | 低保真完整场 | HF校正器额外输入 | 要回答的问题 |
|---|---|---|---|
| E0 | 相同DeepONet | 无 | 当前基线 |
| E1 | 相同DeepONet | 固定 `[2,10,50,200] s` 响应特征 | 普通时间编码是否已足够 |
| E2 | 相同DeepONet | LF训练曲线拟合出的4个时间特征 | LF热响应信息是否提供额外收益 |

不比较E2新排程与E0旧排程后声称是特征收益。排程修复之后三者统一。先seed0筛查，通过者补齐0–4；E0已有合规结果可复用。

如果E2优于E0但没有超过E1，结论应是一般时间表示改善，**不能声称LF提取机制产生独特贡献**。如果E1/E2都不胜过E0，停止加此模块，保留当前模型并转向数据与物理诊断。

### 6.6 新颖性边界

LNO已有极点—留数动态表示；多保真DeepONet已有复合LF/HF建模；在已有方法上增加指数特征不能直接宣称首次。[E2、E3]

E2若稳定有效，贡献应准确写成：针对稀疏实验校正，利用可分辨的低保真热响应对HF校正器进行条件化，并通过等结构对照验证信息来源的价值。这是待检验的方法增量，不是期刊录用保证。投稿前仍需与最接近的响应编码/多保真算子方法逐项核对。

---

## 7. 进入下一阶段的门槛

以下为**预登记研发停止/继续标准，不是期刊门槛或统计显著性标准**。

主选择指标沿用当前 `macro_v1`，不改权重：

\[
S_{val}=\frac{R_{Top}+0.2R_{ring,abs}+R_{ring,\Delta}}{2.2}.
\]

所有量按当前代码的等权功率定义计算，另列各模态，不用有符号均值抵消误差。

建议E2进入主要候选需同时满足：

1. 相对同预算E0的五种子平均S改善至少5%。
2. 至少4/5对应seed改善，报告全部seed，不能删最差。
3. 相对E1也有明确、可重复的增益；若不能超过固定时间特征，则不保留“LF拟合特征贡献”的主张。
4. 任一Top/Hot/Cold的五种子平均RMSE不恶化超过 `max(0.1 ℃, 该模态基线RMSE的5%)`。
5. LF validation全域误差和固定独立physics诊断没有超出预登记容忍度。相对物理残差阈值要结合其数值底噪，不能对接近零的量机械套百分比。
6. 报告推理和训练开销，确认收益不是只来自大幅增加计算预算。

若B0的当前有效验证均值确认为2.00160，则5%研发门槛约为1.90152；这只是算术例子。若B0发生变更，以新的同协议B0为准。不能拿旧测试1.9600设置训练早停目标。

五种子波动是优化重复性，不是五次独立物理实验。3个验证功率也不足以证明总体显著性。上述规则用于限制研发范围，结果全部保留。

---

## 8. 第四张任务单：证明“多保真”和“PINN”都确实有用

### 8.1 必须补的贡献对照

| 编号 | LF标签 | HF Top/Hot/Cold标签 | PDE软损失 | 作用 |
|---|---|---|---|---|
| F0：仅LF | 有 | 无 | 按LF模型已有定义 | 实验校正之前偏差多大 |
| F1：仅HF-PINN | 无 | 有 | 有 | 不依赖仿真的小样本水平 |
| F2：多保真，无PDE软损失 | 有 | 有 | 无 | 多保真数据融合本身的能力 |
| F3：当前多保真PINN | 有 | 有 | 有 | 物理约束的增量 |
| F4：通过E2门槛的候选 | 有 | 有 | 有 | 新特征机制的增量 |

F0直接复用训练完成的LF模型，不需要额外训练。F1/F2初筛seed0，进入正式论文表时补相同五种子。

**公平性要求：**

- F1不能加载LF预训练权重、LF拟合的POD/时间字典、LF派生伪标签或用全部HF拟合出的物理参数。允许使用各方案共同的用户已知几何/物性与预先声明的名义边界；不能偷偷把由LF识别的参数当成“无LF也知道”。
- 可同时给一个受控“同结构随机初始化、无LF监督”消融和一个实用HF-only基线；明确可训练与总参数数，避免把参数量差异全算为数据贡献。
- F2若仍保留硬初值/硬水冷等结构，名称必须是“移除PDE软损失”，不是“完全不含任何物理”。
- F2与F3的LF预训练是否相同必须写明；若LF本身已经是PINN，不能说整个F2从未使用物理。
- 未提供内部实验真值时，不能只凭HF模型与原LF场不接近，判定HF模型错误。

### 8.2 加一个真正接近的文献对照，不再增加一串新网络

优先参考Howard等2023年多保真DeepONet：[E3]。实现或核验其公开架构中的LF/HF复合关系，并接入同一坐标查询和观测协议。

第一项工作是阅读原方法/作者代码并写 `literature_baseline_mapping.md`，说明哪些结构保持、哪些因标量功率输入和局部HF观测而适配。未检查作者实现之前，不把自写普通残差称为精确复现。

原文不是SiC–Cu实验，因此可称“面向本任务的多保真DeepONet适配对照”，不能宣称完全重现原文结果。本项在准备正式投稿表前完成，不阻塞D0和E0/E1/E2先导。

可增加很轻的工程对照：只用训练工况拟合的表面/环温偏差回归或FEM参数校准。仅能预测观测位置的偏差回归不参与内部全域排名；若做完整FEM校准，物理参数只允许训练集估计，成本如实记录。

---

## 9. 第五张任务单：高保真数据效率曲线

目的：量化仿真到底能减少多少真实训练工况，而不是重复声称“高保真少”。多保真温度场研究已有这一研究问题，因此它属于必须有的证据，而非单独创新。[E4]

### 9.1 预先固定训练子集

只从现有12个HF训练功率取子集，验证3个、旧测试3个完全不变；同一子集Top/Hot/Cold同步缩减。

| HF训练工况数 | 主嵌套序列，W |
|---:|---|
| 3 | 55、364.3、729 |
| 6 | 55、216.8、364.3、494.2、593.5、729 |
| 9 | 55、216.8、254.5、364.3、430、494.2、558.5、593.5、729 |
| 12 | 全部12个HF训练功率 |

这些是按功率覆盖预设的设计，不依据温度或模型误差挑选。子集实验不改变原总划分，不是留一功率。

### 9.2 运行矩阵与预算

先只比较：F1仅HF-PINN、F3当前多保真、F4通过门槛的候选。F4未通过则不跑。

- 首轮4种数据量 × 最多3种方法 × seed0，12工况已有结果可复用。
- 若看到数据效率差异，补至少2条独立预登记、仅按功率覆盖构造的嵌套序列作为子集敏感性；不是据结果挑选一条最漂亮的曲线。
- 各方法使用相同子集、同一seed、同预算。独立记录子集选择波动与网络种子波动，不把二者混成一种置信区间。
- 计算资源受限时，先在3/6/12工况补种子；正式完整曲线各点的运行数必须标注，不能单种子点伪装成五种子均值。

### 9.3 防泄漏细节

F1不能用LF字典。所有依赖HF的表面教师、物理参数估计、归一化、早停之外的拟合只能使用该子集。不能删掉Top训练功率，却继续使用未选功率的Hot/Cold。

LF训练60工况固定，可跨各数据量实验复用LF检查点和LF字典。验证3工况的标签也是开发资源：论文应分别报告“HF训练工况数”和“总开发观测量”，不能声称只用了3个实测工况，实际上还用了3个验证工况调参。

输出：误差—HF工况数图、逐点数值与运行数、固定目标精度下所需训练工况数。不提前写“节省50%实验”，只能依据实际曲线、预算定义和不确定性得出。

---

## 10. 第六张任务单：新增一个可全域验证的跨材料数值基准

### 10.1 为什么要做

真实工程只有Top与两个底部环温观测，无法直接知道整个体积场的误差。增加一个拥有完整独立数值参考解的基准，可检查局部观测是否确实约束了隐藏区域；它不能取代真实内部实验。[R4]

### 10.2 最小可行基准：一维双层板瞬态导热

为控制实现成本，先做一个一维两材料厚度方向模型，而不是新增复杂三维工程。

- SiC厚12 mm，Cu厚5.5 mm，总厚17.5 mm。
- 材料常数沿用已经核实的项目配置。
- 初始22 ℃，Cu底部固定22 ℃。
- 顶面施加恒定热通量，`q(P)=eta*P/A`，A与eta在此基准中明确定义并固定。
- 界面使用有限接触热阻；不把两个材料在同位置的温度错误合并。
- 不包含辐射和材料去除，明确这是一维受控基准，不是完整真实装置。

参考求解器独立使用守恒有限体积/有限元时间积分；不可用被比较网络自己的输出生成真值，也不可复用PINN残差当成求解器。

两相邻单元的界面面热流可用热阻串联：

\[
q_{i+1/2}=\frac{T_i-T_{i+1}}{\Delta x_i/(2k_i)+R_c+\Delta x_{i+1}/(2k_{i+1})}.
\]

非材料界面的同材内部面取Rc=0。顶面通量、底部定温和各控制体热容单独实现，做解析稳态热阻验证。

### 10.3 网格收敛与两种失配

先算至少两次网格/时间步加密，参考场误差需显著小于待比较模型差异；建议预登记参考差异低于0.1 ℃且低于所宣称算法差异的10%，达不到则继续加密或缩小结论。

构造两个独立场景：

1. LF同物理但更粗网格/时间步，检验数值保真度差异。
2. LF与参考存在预先固定的Rc失配，例如LF使用参考Rc的0.7倍，同时仍有粗网格差异。这个比例是人为基准条件，不是用户材料真值。

HF物理参数在主基准设为已知，所有PINN方法一致使用；应明确它验证的是已知物理下的部分观测融合，不伪装为未知参数逆问题。将参数未知另列为可选实验，首版不扩展。

### 10.4 部分观测与评价

仅开放顶面以及Cu内部两个不同深度点的HF观测，例如深度13和16 mm；这两个虚拟探针不是实际r=0.028/0.0415 m圆环，不使用Hot/Cold真实环温名称混淆。

训练/验证功率规则参照现有协议提前固定；未见功率参考全域只供最终数值测试。对F1/F2/F3/F4报告：

- 两材料分别的全域温度RMSE，空间用单元体积权重。
- 未观测位置误差；界面两侧温度跃变和热流误差。
- 积分能量平衡；网格与时间步加密误差。
- 已知/有偏LF条件下的收益差异。

至少比较候选与当前DeepONet、HF-only、移除PDE软损失对照。该基准证明的是算法在一个可完整验证问题上的适用性；若重新训练，不能写成原装置模型零样本跨几何泛化。

---

## 11. 第七张任务单：补充真实实验，但只补有用的

### 11.1 什么时候补

软件诊断与方法选择可以先完成。真正用于新模型独立评价的实测数据应在以下内容冻结后采集或封存：模型架构、损失、训练子集、种子、检查点、误差定义、允许的测量校正。

不要边查看新实验误差边选模型，再称这些数据是独立盲测。

### 11.2 建议工况与最小记录

建议示例为180、380、680 W，覆盖当前HF训练范围的低、中、高段，而且不同于现有HF训练/验证/旧测试功率。实际以激光器可稳定实现且测量核实的功率为准，**不能直接将设定功率当作实测功率**。

这些数值只是可执行建议，不是最优实验设计或期刊要求。若这些工况已被观测或用于调试，应预先登记其他未暴露工况。

资源允许时每个功率做2次独立加热试验，共6次；资源更紧时可先做3个新功率各1次，并对其中一个高功率多做1次。后者仍不足以充分估计全范围实验方差，需如实报告。每次应重新回到约定初态，不把同一热像的像素当重复实验。

每次记录：

- 实际测得激光功率、光斑/装配/水冷设置是否保持一致。
- 加热前温度基准、激光开启时间戳、各传感器原始时间表头。
- Top热像与两环温序列；在设备能力内，适当增加0–20 s采样密度，不能通过插值伪造高频实测。
- 环境和冷却温度、试件/装配编号、是否重新装夹、测量精度或校准说明。
- 有无可见材料去除、表面损伤或其他改变物理边界的证据。

新增数据不修改旧12/3/3划分，保存在独立目录。预测必须在读取新增温度标签前生成并记录哈希。若未来选择使用测得初态作为模型输入，应对所有基线和新模型一致开放并声明任务改变。

### 11.3 没有条件新增实验怎么办

继续完成训练/验证研究、数据效率和受控全域基准；旧测试只作已暴露固定留出评价。论文坦诚说明验证边界，不虚构新独立性。不能仅靠更多随机种子代替新的实测工况。

---

## 12. 投稿前最小证据包

目标是补足可审查的贡献，不设置“RMSE低于某数字就能录用”的承诺。EAAI的公开范围强调工程AI创新与可复现验证。[E5]

### 12.1 必备表图

| 产物 | 回答的问题 |
|---|---|
| 数据划分与来源表 | 哪些是真实验、哪些是LF、何时使用 |
| 当前基线与最近文献适配对照表 | 是否只超过弱基线 |
| E0/E1/E2消融表 | LF时间特征是否胜过通用编码 |
| F0/F1/F2/F3/F4表 | 仿真、实验、PDE和新机制分别贡献多少 |
| HF数量—误差曲线 | 是否减少昂贵训练工况需求 |
| 两材料基准全域误差图 | 未观测内部是否可以正确推断 |
| 新实测逐功率/重复试验图 | 泛化与实验重复性证据 |
| 同任务计时表 | 预测完整过程的实际效率 |

三维云图只作为展示，不单独当成准确性证据。

### 12.2 指标

保留原macro_v1选择分数和分别定义的报告指标，不为候选重设权重。主报告至少列：Top/Hot/Cold RMSE、MAE、偏差、P95误差、峰值误差、三个时间窗、最差功率。真实内部无真值时不填内部实验RMSE。

相对误差使用明确的K尺度或温升尺度，并写分母接近零的处理；不只用摄氏绝对温度百分比来夸大精度。对每个完整物理工况先算误差，再按功率/重复试验层次聚合。种子标准差、试验重复标准差、参考数值误差分别报告。

### 12.3 推理计时

在同一空间网格、同一时间范围、同一输出内容下比较模型与FEM/插值。分别报告模型前向、数据读取、格式导出时间；GPU先预热并同步，不用一个点推理与完整FEM流程比较。报告离线训练总成本和预训练可复用性，避免只展示在线加速。

### 12.4 可复现材料

公开可访问的原始或合法处理数据、SHA清单、有效配置、环境锁定、模型构造参数、检查点或重训脚本、参考求解器、结果CSV和失败记录。数据开源权限/许可证由用户确认，不擅自替用户授权。

论文范围注意：输入仅有标量P且几何/边界固定时，应称连续功率的参数化预测，不声称已经证明任意函数热源、任意几何、任意材料泛化。若没有材料去除与移动边界证据，结果是激光加热条件下的温度场预测，不声称预测烧蚀深度或真实烧蚀形貌演化。

---

## 13. 逐文件实施清单

| 文件 | 动作 | 自动测试 |
|---|---|---|
| `configs/optimization_v4.yaml` | 新增所有候选、预算、门槛，默认test禁用 | 合法功率、固定指标与模式检查 |
| `src/sic_cu/models/thermal_response_features.py` | 新增none/fixed/lf_fitted模式 | t=0为0；导数有限；buffer保存重载 |
| `src/sic_cu/data/lf_response_dictionary.py` | 新增LF抽样、字典拟合和元数据 | 仅simulation_train；材料都被消费 |
| `src/sic_cu/models/multifidelity.py` | 新增特征接入，原模式默认不变 | none模式向后兼容；LF监督不触及HF头 |
| `src/sic_cu/train/multifidelity.py` | 统一HF子集入口、诊断和可选排程 | 子集Top/Hot/Cold一致；DDP一致 |
| `src/sic_cu/eval/metrics.py` | 检查窗口、层次聚合、空窗口 | 不覆盖旧口径；可手算小样本 |
| `src/sic_cu/eval/protocol_checks.py` | 扩展字典/子集/新测试来源 | 缓存来源错误立即失败 |
| `scripts/07_export_development_diagnostics.py` | 新增D0 | 禁止test访问 |
| `scripts/07_fit_lf_response_dictionary.py` | 新增离线拟合 | 无HF标签输入 |
| `scripts/08_run_v4_ablation.py` | 新增实验矩阵调度 | 不改变底层physics/data接口 |
| `scripts/08_run_data_efficiency.py` | 新增固定嵌套子集 | 子集间参数与伪标签来源隔离 |
| `benchmarks/two_layer_heat/` | 新增独立参考与收敛脚本 | 稳态解析热阻、守恒、网格加密 |
| `reports/development_v4/decision_log.md` | 每个决策记录证据/失败 | 不将未完成写成通过 |

上述新增项均为拟议文件，不代表已经编写或运行。实现时复用现有公共模块，禁止复制多个逐渐不一致的训练器。不要改动已有冻结发布结果。

---

## 14. 建议配置草案

以下是方案参数，不是已经调出的最优配置。实际硬件、步数与模型预算先继承可追溯B0。

```yaml
schema_version: 1
round_id: development_v4
source_commit: bbe563e05ee65e28362ccab04d91773896d8d0c4
protocol_id: hf_fixed_12_3_3_v4
allow_test_labels: false
allow_logo: false
preserve_frozen_releases: true

models:
  backbone: existing_deeponet_low_fidelity
  correction: existing_coordinate_lf_temperature_mlp
  material_aware: true
  surface_hard_guide: false

fidelity:
  lf_training_scope: simulation_train_full_sic_and_cu
  hf_training_scope: selected_hf_train_top_hot_cold
  simulation_supervision_target: low_fidelity
  physics_supervision_target: high_fidelity
  freeze_lf_in_initial_screen: true

features:
  candidate_modes: [none, fixed_response, lf_fitted_response]
  response_count: 4
  fixed_tau_s: [2.0, 10.0, 50.0, 200.0]
  source_nodes_per_material_per_power: 32
  fit_from_hf: false
  fit_dictionary_dtype: float64
  freeze_dictionary_in_hf: true

experiments:
  screen_seeds: [0]
  confirmation_seeds: [0, 1, 2, 3, 4]
  max_promoted_feature_candidates: 2
  primary_selection_metric: macro_v1
  require_paired_lf_checkpoints: true
  log_trainable_and_total_parameters: true
  no_physics_label: remove_pde_soft_loss

stopping:
  relative_mean_validation_gain: 0.05
  improved_paired_seeds_at_least: 4
  per_modality_degradation_abs_c: 0.1
  per_modality_degradation_relative: 0.05
  modality_tolerance_rule: maximum_of_abs_and_relative
  compare_against_fixed_response: true
  thresholds_are_journal_requirements: false

hf_budget_subsets:
  n3: [55.0, 364.3, 729.0]
  n6: [55.0, 216.8, 364.3, 494.2, 593.5, 729.0]
  n9: [55.0, 216.8, 254.5, 364.3, 430.0, 494.2, 558.5, 593.5, 729.0]
  n12: [55.0, 216.8, 254.5, 257.0, 309.0, 364.3, 430.0, 494.2, 532.0, 558.5, 593.5, 729.0]
```

配置解析时已有全局12工况断言不应被删除。应新增“全局协议不变、训练子集是合法子集”的独立实验入口；校验整份源数据仍满足12/3/3，而某次数据效率运行只消费声明的train子集。

---

## 15. 里程碑和停止条件

| 阶段 | 必须先做 | 完成后交付 | 不通过时 |
|---|---|---|---|
| M0 | 来源冻结与D0诊断 | 诊断CSV、曲线、源数据清单、有效配置 | 修复数据/导数/指标问题，不训新方法 |
| M1 | 本轮B0锁定 | 基线清单、验证复现、排程决策 | 保留现有方案，记录差异 |
| M2 | E0/E1/E2单因素先导 | 同预算seed0比较与LF字典诊断 | 新特征无收益则停止该分支 |
| M3 | 候选五种子确认 | 所有seed验证表、门槛判定 | 不声称有效创新；不得查test寻优 |
| M4 | F0–F4与数据效率 | 贡献表、预算曲线、子集敏感性 | 收缩对应贡献，不编造节省比例 |
| M5 | 两材料完整参考基准 | 收敛证据、未观测区域误差 | 修复物理/表示；不能用云图替代 |
| M6 | 冻结后新增实验与发布 | 新实测报告、计时、可复现包 | 无新增数据则保留受限的验证结论 |

不要求所有分支必须成功。目标是用尽量少的改动获得可归因证据，不是生成更多算法名字或更多有利表格。

---

## 16. 可直接发送给 Codex 的第一条指令

```text
请在现有 temperature-field-prediction 工程上建立 development_v4 分支，以 V4 计划为准增量实施。
保留12/3/3高保真和60/10/10低保真固定协议，不恢复LOGO，不读取test_Data温度，不覆盖20260909冻结结果。

本次先只完成 M0/M1：
1. 核对本地代码、有效CLI、LF/HF检查点和数据指纹；不要用新提交号冒充旧dirty源码。
2. 复用现有DeepONet多保真模型，导出训练/验证的逐功率Top/Hot/Cold、时间窗、径向误差、LF保持误差和独立物理诊断。
3. 核对时间表头、初态、温升参考时刻；无训练证据不得加传感器偏置或删除首帧。
4. 记录各loss的有效步数、梯度量级和共享参数夹角；如果物理更新失衡有证据，单独对比排程，不同时改网络。
5. 新增全局协议不变的HF训练子集接口和测试标签访问拦截；原12/3/3源数据断言不能删除。
6. 生成 reports/development_v4/diagnostics/ 和 baseline_manifest.yaml，明确哪些是事实、假设和未完成项。

这一阶段不要训练新PRC，不要增加FNO/GNO/扩散模型，不要开始新测试评价。
数据与导数检查通过且B0锁定后，下一项才是E0/E1/E2：无额外特征、固定4个响应时间特征、仅由LF训练场拟合的4个时间特征。
所有候选沿用同一低保真完整场和跨材料物理约束；LF标签只监督LF输出，Top/Hot/Cold监督最终HF输出。
```

---

## 17. 来源与相邻工作

### 已核对项目来源

- [R1] 当前结果报告：`reports/final_test_20260909_user_authorized/README.md`。固定提交链接：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/reports/final_test_20260909_user_authorized/README.md
- [R2] DeepONet验证清单：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/reports/releases/user_authorized_test_20260909_v1_deeponet/checkpoint_index.json
- [R3] 多保真模型代码：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/src/sic_cu/models/multifidelity.py
- [R4] 固定数据协议：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/DATA_USAGE_PLAN.md
- [R5] 多保真训练器：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/src/sic_cu/train/multifidelity.py
- [R6] 历史冻结清单：
  https://github.com/fei4440273/temperature-field-prediction/blob/bbe563e05ee65e28362ccab04d91773896d8d0c4/reports/releases/user_authorized_test_20260909_v1_deeponet/release_manifest.yaml
- 本轮亦复核了此前交付的 `temperature_field_prediction_results_review_v3.md`。未假定其建议已经执行。

### 方法机制与投稿准备参考

- [E1] Wang, S., Teng, Y., Perdikaris, P. *Understanding and Mitigating Gradient Flow Pathologies in Physics-Informed Neural Networks*. SIAM Journal on Scientific Computing, 2021, 43(5), A3055–A3081. DOI: https://doi.org/10.1137/20M1318043 。作者预印本：https://arxiv.org/abs/2001.04536 。用途：梯度诊断的机制依据，不将原文数值改善套用到本项目。
- [E2] Cao, Q., Goswami, S., Karniadakis, G. E. *Laplace neural operator for solving differential equations*. Nature Machine Intelligence, 2024. DOI: https://doi.org/10.1038/s42256-024-00844-4 。用途：极点—留数/瞬态表示的相邻工作；本文特征模块不是原始LNO复现。
- [E3] Howard, A. A., Perego, M., Karniadakis, G. E., Stinis, P. *Multifidelity deep operator networks for data-driven and physics-informed problems*. Journal of Computational Physics, 2023, 493, 112462. DOI: https://doi.org/10.1016/j.jcp.2023.112462 。作者预印本：https://arxiv.org/abs/2204.09157 。用途：必须区分的最近方法类别及适配对照。
- [E4] Zhang, Y., Gong, Z., Zhou, W., Zhao, X., Zheng, X., Yao, W. *Multi-fidelity surrogate modeling for temperature field prediction using deep convolution neural network*. Engineering Applications of Artificial Intelligence, 2023, 123, 106354. DOI: https://doi.org/10.1016/j.engappai.2023.106354 。作者预印本：https://arxiv.org/abs/2301.06674 。用途：多保真温度场与数据效率研究已有先例，不能仅靠数据种类声称创新。
- [E5] Engineering Applications of Artificial Intelligence，官方作者指南：
  https://www.sciencedirect.com/journal/engineering-applications-of-artificial-intelligence/publish/guide-for-authors 。查阅日期2026-09-09；用途：工程AI贡献与复现准备，不作为任何固定精度/工况数量/录用概率的依据。

**最终成功标准：明确说明哪个机制在什么条件下有效，并用独立于开发过程的证据支持；不是必须让某个新简称赢，也不是让已暴露测试集的分数不断降低。**
